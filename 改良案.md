# 実装済みの改良内容

## 環境・センサー契約

- FSRセンサーの取得範囲をXMLの宣言順に修正
  - IMU後の `sensordata[10:18]` を使用
  - 左足4ch、右足4chの順序に統一
- `RobotConfig.FSR_POSITIONS` をXMLのFSRサイト配置に合わせて修正
- XMLのセンサー順序を検証する回帰テストを追加
- actuator順とMuJoCoのqpos/qvel順の変換を追加
- 関節位置・速度の観測をactuator順に統一
- 関節順序ABIの回帰テストを追加

## 報酬・metrics

- `reset()` と `step()` のmetrics pytree構造を統一
- `reward_is_finite` をreset時のmetricsにも追加
- 報酬のNaN/Inf検知を追加
- 非有限報酬を検知した場合の終了・ペナルティ処理を追加
- 報酬計算側のFSR取得順序をXMLと統一

## Domain Randomization

- 質量スケールをMJXモデルの `body_mass` に反映
- 摩擦スケールを `geom_friction` に反映
- COMオフセットを `body_ipos` に反映
- DR設定が単なるprivileged observationではなく、物理モデルへ適用されるよう修正

## 評価・学習契約

- 直接評価時の `training_progress` を `1.0` に設定
- 評価時にsoft penaltyがゼロ固定になる問題を修正
- deterministic / stochastic評価用の診断経路を追加
- 終了stepヒストグラムと報酬成分分解の診断処理を追加
- PPO方策のmean soft clipを実装
- policy standard deviationを `0.05`〜`3.0` に制限
- mean/std境界の検証テストを追加
- JAX永続コンパイルキャッシュ設定を追加

## PPO・終了処理の検証

- PPOのKL計算軸を確認・修正
- BraxのGAE/bootstrap処理を確認
- `time_out` とhorizon 500stepの配線を確認
- 報酬の各stepクリップ範囲を確認

## ロボット・実機経路

- gait parameterとinitial heightの設定を整理
- kinematicsのリンク長単位と到達範囲判定を修正
- JAX/NumPyのクォータニオン処理を分離し、JIT互換化
- 実機側の位相同期とFIFO履歴を整合
- Teensy通信のchecksum検証を厳格化
- CBFのmargin計算とペナルティ基準を統一

## 検証状況

- 編集対象ファイルの静的エラー検査は通過
- センサー契約テストと評価契約テストを追加
- GPU/WSLでのpytestおよび本番学習は、実行環境の制約により未完了
多数のファイルを突き合わせて確認しました。実際に矛盾・不整合が見つかった箇所を重要度順にまとめます。

## 🔴 重大（動作・学習結果に直接影響しうる）

### 1. `reset()` と `step()` で metrics 辞書のキー集合が一致していない
`envs/mjx_env.py` の `reset()` で初期化している metrics には **`reward_is_finite` キーが存在しません**。

- `envs/mjx_rewards.py` の `compute()` が返す metrics には `reward_is_finite` が含まれる
- `reset()` 側の初期化リストには `stability_metrics_finite`, `com_accel_is_fallback`, `action_saturation` など他の診断キーは律儀に追加されているのに、`reward_is_finite` だけ漏れている

Brax/JAXは `reset()` が返す pytree 構造（キー集合・shape）を基準にトレースするため、`step()`側だけキーが増えていると `jax.lax.scan` 等で構造不一致エラーになる、または黙って型チェックをすり抜けて別の不具合を招くリスクがあります。実際、コード内コメントでも他のキーについては「pytree構造を一致させる必要がある」と明記されているのに、この1つだけ抜けています。

### 2. `KP`/`KD` の設定値がXMLに反映されていない
`robot/config.py`:
```python
# PD制御ゲイン (Sim用) — 外乱耐性のため剛性を引き上げ
KP = 40.0
KD = 1.0
```
しかし `assets/humanoid/humanoid.xml`（およびそのコピーである `humanoid_visualize.xml`、トップレベルの `assets/humanoid.xml`、`mjx_env.py` のfallback XML）は全て：
```xml
<position name="right_hip_yaw" ... kp="20" kv="0.5" .../>
```
**config.pyの「剛性を引き上げた」コメントは実機には全く反映されておらず、実際に使われているのは古い kp=20, kv=0.5 のままです。** KP/KDはコード上どこからもXMLに書き込まれていない（PDゲインをXMLへ動的注入する仕組みがない）ため、この定数は事実上死んでいます。

### 3. `TERMINATION_HEIGHT` の座標系がコメントと実装で食い違っている
`robot/config.py`:
```python
INITIAL_HEIGHT = 0.1773  # 直立姿勢での重心高さ（胴体位置）
TERMINATION_HEIGHT = INITIAL_HEIGHT - 0.07  # 初期高さから7cm低下したら終了
```
これは「world絶対座標の胴体高さ」を基準にした設計コメントです。ところが `envs/mjx_rewards.py` では：
```python
lowest_foot_z = jp.minimum(left_foot_pos[2], right_foot_pos[2])
relative_height = base_pos[2] - lowest_foot_z  # 足裏基準の相対高さ
...
is_low = relative_height < RobotConfig.TERMINATION_HEIGHT
```
`docs/current.md`の契約（「高さは足裏を基準にする」）に合わせて "Contract Violation B" として相対高さ実装に修正済みなのに、`config.py`側の**変数名・コメント・計算式（`INITIAL_HEIGHT - 0.07`）は絶対座標系の古い設計思想のまま**残っています。足が地面に密着していれば数値的にはほぼ一致するため実害は出にくいですが、意味論としては矛盾しています。

## 🟡 中程度（DRY違反・値のズレ、将来バグの温床）

| 項目 | config.py 側 | 実際に使われている値 |
|---|---|---|
| `MJX_NUM_ENVS`/`BATCH_SIZE`/`UNROLL_LENGTH` | 2048 / 1024 / 20 | `train/train_mjx.py` のargparseデフォルトは 256 / 256 / 10（config.py側は完全に未参照＝死んだ設定） |
| `TOTAL_TRAINING_STEPS_ESTIMATE` | 25,000,000（USE_REFERENCE_GAIT=False時） | `train_mjx.py` のGPUデフォルト `steps=10_000_000`。カリキュラム進捗計算の分母が2系統存在 |
| `GAIT_STAND_HEIGHT` | 0.23m | 実際の直立姿勢高さ`INITIAL_HEIGHT`=0.1773m、報酬計算の`COM_HEIGHT`=0.17m。USE_REFERENCE_GAIT=Trueにした瞬間に矛盾が顕在化する |
| `NUM_JOINTS`/`BASE_OBS_DIM`/`OBS_DIM` | `len(JOINT_NAMES)`等から計算 | `real/real_env.py`のクラス変数として `20`/`84`/`625`が**独立にハードコード**（値は現状一致だが同期機構なし） |
| `mjx_env.py reset()`の `qpos.at[0:3].set([0,0,0.1773])` | `RobotConfig.INITIAL_HEIGHT`は使わず直値0.1773をハードコード | 同様に `scratch/gate0_formal_eval.py` 等でも`0.1773`が複数箇所に直書き |

## 🟢 軽微（未使用・重複定義。バグではないが要整理）

- **`ARM_SWING_LIMIT_DEG` / `ALLOW_ARM_SWING`**（config.py）… `mjx_env.py`/`mjx_rewards.py`のどこからも参照されていない
- **`NOISE_ANGULAR_POS` / `NOISE_ANGULAR_VEL` / `NOISE_IMU_ANGLE` / `NOISE_IMU_GYRO`**（config.py）… 実際に使われているのは`NOISE_BASE_POS`/`NOISE_LIN_VEL`のみ
- **`REWARD_WEIGHTS["symmetry"]`**… 重み0.0のまま、report計算に一切登場しない
- **`FSR_CONTACT_THRESHOLD`(=0.5) vs `FOOT_CONTACT_THRESHOLD`(=0.05)**… 似た名前の閾値が2つ存在し、実際に使われているのは後者のみ
- **`PRIVILEGED_OBS_DIM`/`privileged_obs`**… `reset()`で計算・`info`に格納されるが、最終観測ベクトル(`final_obs`)には一切含まれない＝RMA廃止時の名残りの死にコード
- **`assets/humanoid.xml`（トップレベル）**… `RobotConfig.MUJOCO_MODEL_PATH`が指すのは`assets/humanoid/humanoid.xml`なので、トップレベル版は使われていない重複ファイル（中身も微妙にrgba属性の有無が違う）
- **`self.latency_steps`**（mjx_env.py）… コンストラクタで受け取り保存されるが、`step()`内では別ロジック（`jax.random.randint`による独自遅延）が使われており未参照

---
最も直すべきなのは**①のmetrics pytree不整合**（潜在的にjax.lax.scan/vmapでの実行時エラーや誤集計につながる）と、**②のKP/KD不一致**（意図した剛性が実は学習・実機どちらにも反映されていない）です。どれか特定の箇所から着手しますか？(改良規約上、1回の変更は1カテゴリに絞るのが良さそうです)
さらに深く掘りました。前回見落としていた、かなり重大な不整合が2つ見つかりました。

## 🔴🔴 最重大（新規発見）

### A. `data.sensordata[:8]` が実は足裏センサーではなくIMUデータを拾っている

`humanoid.xml` の `<sensor>` ブロックの宣言順は：
```xml
<gyro name="torso_gyro" .../>            <!-- index 0-2 (3値) -->
<accelerometer name="torso_accel" .../>  <!-- index 3-5 (3値) -->
<framequat name="torso_quat" .../>       <!-- index 6-9 (4値) -->
<touch name="sensor_l_foot_fl" .../>     <!-- index 10 -->
<touch name="sensor_l_foot_fr" .../>     <!-- index 11 -->
... (計8個のtouchセンサーは index 10〜17) -->
```
MuJoCoのsensordataは宣言順にフラット結合されるので、touch(FSR)センサーは **index 10〜17** です。

ところが `envs/mjx_env.py` の `_get_obs()`：
```python
if nsensor >= 8:
    fsr_data = data.sensordata[:8]   # ← index 0〜7 を取得
```
これは実際には `[gyro_x, gyro_y, gyro_z, accel_x, accel_y, accel_z, quat_w, quat_x]` であり、**FSRタッチ値は一つも含まれていません**。

さらに `envs/mjx_rewards.py` の `compute()`：
```python
left_foot_force = jp.where(has_sensors, jp.clip(jp.mean(jp.abs(data.sensordata[0:4] + 1e-6)), 0.0, 100.0), 0.5)
right_foot_force = jp.where(has_sensors, jp.clip(jp.mean(jp.abs(data.sensordata[4:8] + 1e-6)), 0.0, 100.0), 0.5)
```
同じバグで、`left_foot_force`/`right_foot_force` は実際には `[gyro×3, accel×1]` と `[accel×2, quat×2]` の平均絶対値です。これが波及する範囲：
- 観測ベクトルの `fsr_data`（8次元）と、そこから計算される `zmp`（2次元）が丸ごとノイズ
- `both_feet_contact` 報酬（重み8.0）
- `stability_metrics.py` に渡る `left_foot_force`/`right_foot_force` → `foot_balance`, `zmp_margin`, `stability_index` 全部
- `swing_is_left`（Capture Point計算のswing/stance判定）
- disturbance recovery bonusのゲーティング条件

`docs/current.md` には「シミュレーション内のFSR連続値・ZMP指標は学習／評価専用」と書かれており、これは「連続値だが実際の接触力を表す」ことを前提にした記述なので、設計意図とも矛盾しています。既存の単体テスト（`test_improved_rewards.py`等）は全て手動構築したダミー入力を使っており、実際のXML＋MJXパイプラインを通していないため、このバグを検出できていません。
`humanoid.xml` の `<worldbody>` 内のbody入れ子順序は：
```
左腕(4関節) → 右腕(4関節) → 左脚(6関節) → 右脚(6関節)
```
なのでMuJoCoの `data.qpos[7:]` / `data.qvel[6:]` は**このツリー順**で並びます。

一方 `RobotConfig.JOINT_NAMES` / `DEFAULT_JOINT_ANGLES`、`<actuator>` ブロック、そして `real/real_env.py` の `joint_pos = self.smoothed_action`（actuator順で計算される）は：
という**全く別の並び順**です。

`envs/mjx_env.py::_get_obs()`：
```python
joint_pos = data.qpos[7:]   # ツリー順: 左腕,右腕,左脚,右脚
joint_vel = data.qvel[6:]   # 同上
```
```python
jnt_id = sys_mj_model.actuator_trnid[act_i][0]
qpos_adr = sys_mj_model.jnt_qposadr[jnt_id]
actuator_to_qpos_list.append(qpos_adr)   # actuator順→qposアドレスへの正しい変換
```
初期姿勢設定(`reset()`)はこの`_actuator_to_qpos_idx`でactuator順→qpos順に正しく変換しているのに、観測取得(`_get_obs()`)では同じ変換をせず生の`qpos[7:]`を使っています。**同一ファイル内で処理の一貫性が取れていない**ことが、これがバグである強い状況証拠です。

---
3. **サンプルされるが一度も消費されないドメインランダム化変数が3つ**
   - `info['dr_kp_scale']`（reset()で`0.7〜1.3`をサンプル）… step()内で一切参照されず、アクチュエータ剛性のランダム化は実質機能していない
   - `info['disturbance_scale']`（reset()で`0.0〜1.0`をサンプル）… step()内で未参照
   - `info['action_buffer']`（reset()でゼロ初期化）… step()内で未参照

4. **`MJXRewardSystem.compute()`の引数`reference_action`が関数内で一度も使われていない**（デッドパラメータ）。`mjx_env.py`は`info['reference_action']`を毎ステップ計算してこの引数に渡しているが無意味。

   - `scratch/phase0_eval_diagnostics.py`の出力JSONが自己矛盾：`"directions": 8`と`"implementation": "...direction is sampled continuously"`を同じレポート内に両方出力している

6. **カリキュラムスケール計算が2ファイルに完全重複**：`mjx_env.py::_get_curriculum_scale()`と`mjx_rewards.py::_get_curriculum_disturbance_scale()`は実装が一字一句同じ。前者は実際の外力に使われ、後者はログ用metricsにしか使われていない（今は結果一致するが、将来どちらかだけ編集されるとズレる）。


## 🟢 軽微（新規／再確認）
- `self._actuator_indices`（mjx_env.py `__init__`）… 計算されるがクラス内どこからも参照されない
- `RobotConfig.REWARD_WEIGHTS['symmetry']`… 全`w[...]`アクセス箇所を洗い出して確認、確定で未使用
- `RobotConfig.FSR_CONTACT_THRESHOLD`(0.5)… 実際に使われているのは別名の`FOOT_CONTACT_THRESHOLD`(0.05)のみ（前回指摘済みの再確認）

---
## 🔴🔴 新規の重大発見

```python
angle_deg = np.clip(angle_deg, -120.0, 120.0)  # ±120度ハードクランプ
pos = int(np.clip((angle_deg + 120.0) / 240.0 * 1000.0, 0, 1000))
```
これは意図的なコメント付きで「±120度」に固定クランプしています（Hiwonderシリアルサーボの一般的な総回転角240度に対応する、物理的な仕様上の制約と思われます）。

ところが `robot/config.py` / `humanoid.xml` の `JOINT_LIMITS` は、これを超える可動域を許容している関節が複数あります：
- `right_hip_yaw`: `[0°, 180°]`
- `left_hip_yaw`: `[-180°, 0°]`
- `right_shoulder_roll` / `left_shoulder_roll`: `[-180°, 180°]`
- `right_shoulder_pitch` / `left_shoulder_pitch`: `[0°, 180°]` / `[-180°, 0°]`
- `right_elbow` / `left_elbow`: `[0°, 180°]` / `[-180°, 0°]`

さらに変換式 `(angle_deg + 120.0) / 240.0 * 1000.0` は「0ラジアン＝サーボ中央（pos=500）」を前提にしていますが、上記の非対称な関節（例：`right_hip_yaw`は`[0°,180°]`で0ラジアンが可動域の**端**）ではこの前提が成立しません。**シミュレーションで学習した方策がこれらの関節に大きな角度を指令しても、実機では120度でクランプされ、しかも変換式自体が関節ごとのオフセットを考慮していない**ため、sim-to-realで挙動が大きくズレる可能性が高いです。

### D. `RobotConfig.FSR_POSITIONS` の座標が実際のXML内センサー配置と一致しない
`robot/config.py`:
```python
FSR_POSITIONS = np.array([
    [-0.08, -0.04], [0.08, -0.04], [-0.08, 0.04], [0.08, 0.04],  # Right foot
    [-0.08,  0.04], [0.08,  0.04], [-0.08, -0.04], [0.08, -0.04],  # Left foot
])
```
一方、`humanoid.xml` に実際に定義されているFSRサイト座標：
```xml
<site name="l_foot_fsr_fl" pos="0.012 0.027 -0.0541" .../>
<site name="l_foot_fsr_fr" pos="-0.012 0.027 -0.0541" .../>
<site name="l_foot_fsr_bl" pos="0.012 -0.070 -0.0541" .../>
<site name="l_foot_fsr_br" pos="-0.012 -0.070 -0.0541" .../>
```
スケールも(±0.08m vs ±0.012m)、非対称なy範囲(-0.070〜0.027)も全く一致していません。さらに `mjx_env.py::_get_obs()` のZMP計算：
```python
foot_positions = jp.array(RobotConfig.FSR_POSITIONS)
zmp_x = jp.sum(foot_positions[:, 0] * fsr_data) / total_p
```
は、左右の足を**同じローカル座標系**として扱っており、実際の足のワールド座標オフセット（両足の間隔）を一切考慮していません。左右の足がどれだけ離れていても計算結果が変わらない設計です（※もっとも、`fsr_data`自体が前回発見のバグAで既に汚染されているため、この座標ズレは「二重に壊れている」状態です）。

### E. Domain Randomizationの減衰・摩擦係数がactuator順で生成されるのに、joint velocityはツリー順に適用されている（バグBの波及）
前回報告した「`joint_pos`/`joint_vel`の並び順がactuator順とXMLツリー順でズレている」バグの追加の影響範囲です。`mjx_env.py::step()`：
```python
'dr_damping': jax.random.uniform(key_dr1, shape=(self._mjx_model.nu,), ...),  # ← actuator順で生成
'dr_friction': jax.random.uniform(key_dr2, shape=(self._mjx_model.nu,), ...),
...
joint_vel = state.pipeline_state.qvel[6:]  # ← ツリー順（左腕,右腕,左脚,右脚）
damping_torque = -info['dr_damping'] * joint_vel
friction_torque = -info['dr_friction'] * jp.sign(joint_vel)
qfrc_applied = qfrc_applied.at[6:].add(damping_torque + friction_torque)
```
`dr_damping[0]`は本来「right_hip_yaw用の減衰係数」のつもりで生成されていますが、実際には`qvel[6]`（ツリー順で最初に来る左腕の関節）に適用されてしまいます。ドメインランダム化が意図した関節と全く違う関節にかかっているため、このDRが「関節ごとの個体差を模倣する」という目的を果たせていません。

---

## 🟡 その他、確認して見つけた軽微な点

- `envs/mjx_rewards.py::_compute_disturbance_recovery_bonus()` の`getattr(RobotConfig, 'RECOVERY_BONUS_STABILITY_THRESHOLD', 0.7)` — フォールバックのデフォルト値(0.7)が、実際に`config.py`で設定されている値(0.4)と食い違っている（実害はないが、設定が消えた場合に古い値へ静かにフォールバックする）
- `scripts/visualize_mujoco.py` / `scratch/render_collision.py` はXMLロード後に `model.opt.timestep` を`RobotConfig.SIM_DT`で上書きしていない（`scratch/gate0_mujoco_eval.py`は上書きしている）ため、XML内の`timestep="0.002"`のまま動く＝学習時のタイムステップ(0.0025)と可視化時のタイムステップが異なる
- `gate0_formal_eval.py` と `gate0_mujoco_eval.py` は同じ「Gate 0」判定なのに合格基準の項目セットが微妙に異なる（`foot_touch_rate`基準 vs `both_foot_contact_rate`+`torque_saturation_rate`基準）

---

特に **C（±120度クランプと関節可動域の矛盾）** と **D/E（FSR座標・DR適用先のミスマッチ）** は、前回報告したA・Bと合わせると「実機投入時に足回り・上半身の広可動域関節でシミュレーションと全く違う動きになる」「外乱への個体差ランダム化が事実上機能していない」という、実機移行前に必ず潰しておくべき問題群になっています。

さらに掘りますか？（メッシュ慣性値の物理的妥当性や、`gait_generator.py`/`kinematics.py`側—現状`USE_REFERENCE_GAIT=False`で未使用だが将来有効化した場合の整合性—はまだ精査していません）
GAIT_THIGH_LEN/GAIT_KNEE_LEN の実寸との乖離を確認できました。これがどう波及するか、実際に歩容生成コードを動かして検証します。さらに検証しました。これは前回の「STAND_HEIGHTが最大リーチを超過している」という私の予想より、実は**もっと根が深いバグ**でした。GAIT_STAND_HEIGHT のデッドパラメータバグを実証できました。これで `gait_generator.py`/`kinematics.py` 周りの全体像がはっきりしました。まとめます。

## 🔴🔴 新規の最重大発見

### F. `GAIT_STAND_HEIGHT` が計算に一切使われていないデッドパラメータ（実証済み）
`robot/gait_generator.py` の `jax_cycloid_trajectory(phase, foot_height, step_length, stand_height)`：
```python
def jax_cycloid_trajectory(phase, foot_height, step_length, stand_height):
    ...
    z_swing = (foot_height / np.pi) * (1.0 - jp.cos(theta_swing))
    ...
    z_traj = jp.where(is_swing, z_swing, z_stance)
    return x_traj, z_traj
    # stand_height を使う行がソースコード上のどこにも存在しない
```
`stand_height`（呼び出し元では `RobotConfig.GAIT_STAND_HEIGHT=0.23`）を実際に `0.23` と `999.0` に差し替えて実行し、**出力が完全に同一になる**ことを実証しました。つまり「腰の高さまで脚を伸ばす」という歩容の根幹部分が実装から欠落しており、IKに渡される目標z座標は常に `[0, foot_height=0.04]` という極小レンジのみです。同じロジックが `GaitGenerator.get_foot_position()`（NumPy版、`real_env.py`が実機で使う経路）にも存在し、`self.stand_height`は保存されるだけで一度も参照されません。**シミュレーション側・実機側どちらの参照歩容も、同じ根本原因で「膝がほとんど曲がらない、腰高もゼロの棒立ち軌道」になります。**（現在は`USE_REFERENCE_GAIT=False`のため発現していない潜伏バグ）

### G. `GAIT_THIGH_LEN`/`GAIT_KNEE_LEN`（各0.12m）が実機XMLのリンク長と約43%乖離
左脚の関節位置をXMLからそのまま順運動学で解いた結果：
```
THIGH長 (hip_pitch → knee)   = 68.70 mm   ← config.py想定は 120.00 mm
SHIN長  (knee → ankle_pitch) = 68.70 mm   ← config.py想定は 120.00 mm
```
`kinematics.py`の`LegKinematics.L_THIGH/L_SHIN`も同じ`RobotConfig.GAIT_THIGH_LEN/GAIT_KNEE_LEN`を参照しているため、実機/CADの実寸を全く反映していません。仮にF（デッドパラメータ）を直しても、この値のままではIKの解が実際のロボット形状と一致しません。

さらに副産物として、`GAIT_STAND_HEIGHT=0.23m`は**config.py上の理論最大リーチ(0.12+0.12=0.24m)にすら収まらず**、実測リーチ(0.1374m)の約1.7倍を要求する値になっています。Fのバグで実害は隠れていますが、Fだけ直すと即座にこちらの限界超過が表面化する「二重の地雷」構造です。

## 🟡 追加の中程度所見（既存の指摘との関連）

### H. `ACTION_SCALE`(±30°)は多数の関節でCBF介入が常態化する設計になっている
`DEFAULT_JOINT_ANGLES`基準で`action=±1.0`を与えたときの飽和状況を全関節で計算：
```
right_hip_yaw / left_hip_yaw / right_ankle_roll / left_ankle_roll
right_knee / left_knee / right_shoulder_pitch / left_shoulder_pitch
right_elbow / left_elbow / right_wrist_pitch / left_wrist_pitch  → いずれも★飽和
```
20関節中12関節が、ごく普通の大きさの行動（|action|=1.0）で可動域を超えます。これ自体はCBFが対処する設計かもしれませんが、前回報告した「Action Distortion」監査（`action_saturation_max=0.5`＝平均飽和率50%超で警告）と組み合わせると、**この閾値は関節配置上ほぼ常時トリガーされる可能性が高く**、実際の方策の異常検知として機能しない（常時アラート＝実質無効化）懸念があります。

---

以上でひとまず主要ファイル（config.py, mjx_env.py, mjx_rewards.py, stability_metrics.py, safety/cbf.py, real_env.py, real_io.py, gait_generator.py, kinematics.py, humanoid.xml, training_wrapper.py, train_mjx.py）は一通り相互突き合わせが完了しました。残っている未精査領域は、`deploy/export_onnx.py`（ONNX変換パイプライン）と、各STLメッシュの慣性値(`<inertial mass=... fullinertia=...>`)の物理的妥当性くらいです。そちらも掘りますか？
多数のファイルを突き合わせて確認しました。実際に矛盾・不整合が見つかった箇所を重要度順にまとめます。

## 🔴 重大（動作・学習結果に直接影響しうる）

### 1. `reset()` と `step()` で metrics 辞書のキー集合が一致していない
`envs/mjx_env.py` の `reset()` で初期化している metrics には **`reward_is_finite` キーが存在しません**。

- `envs/mjx_rewards.py` の `compute()` が返す metrics には `reward_is_finite` が含まれる
- `reset()` 側の初期化リストには `stability_metrics_finite`, `com_accel_is_fallback`, `action_saturation` など他の診断キーは律儀に追加されているのに、`reward_is_finite` だけ漏れている

Brax/JAXは `reset()` が返す pytree 構造（キー集合・shape）を基準にトレースするため、`step()`側だけキーが増えていると `jax.lax.scan` 等で構造不一致エラーになる、または黙って型チェックをすり抜けて別の不具合を招くリスクがあります。実際、コード内コメントでも他のキーについては「pytree構造を一致させる必要がある」と明記されているのに、この1つだけ抜けています。

### 2. `KP`/`KD` の設定値がXMLに反映されていない
`robot/config.py`:
```python
# PD制御ゲイン (Sim用) — 外乱耐性のため剛性を引き上げ
KP = 40.0
KD = 1.0
```
しかし `assets/humanoid/humanoid.xml`（およびそのコピーである `humanoid_visualize.xml`、トップレベルの `assets/humanoid.xml`、`mjx_env.py` のfallback XML）は全て：
```xml
<position name="right_hip_yaw" ... kp="20" kv="0.5" .../>
```
**config.pyの「剛性を引き上げた」コメントは実機には全く反映されておらず、実際に使われているのは古い kp=20, kv=0.5 のままです。** KP/KDはコード上どこからもXMLに書き込まれていない（PDゲインをXMLへ動的注入する仕組みがない）ため、この定数は事実上死んでいます。

### 3. `TERMINATION_HEIGHT` の座標系がコメントと実装で食い違っている
`robot/config.py`:
```python
INITIAL_HEIGHT = 0.1773  # 直立姿勢での重心高さ（胴体位置）
TERMINATION_HEIGHT = INITIAL_HEIGHT - 0.07  # 初期高さから7cm低下したら終了
```
これは「world絶対座標の胴体高さ」を基準にした設計コメントです。ところが `envs/mjx_rewards.py` では：
```python
lowest_foot_z = jp.minimum(left_foot_pos[2], right_foot_pos[2])
relative_height = base_pos[2] - lowest_foot_z  # 足裏基準の相対高さ
...
is_low = relative_height < RobotConfig.TERMINATION_HEIGHT
```
`docs/current.md`の契約（「高さは足裏を基準にする」）に合わせて "Contract Violation B" として相対高さ実装に修正済みなのに、`config.py`側の**変数名・コメント・計算式（`INITIAL_HEIGHT - 0.07`）は絶対座標系の古い設計思想のまま**残っています。足が地面に密着していれば数値的にはほぼ一致するため実害は出にくいですが、意味論としては矛盾しています。

## 🟡 中程度（DRY違反・値のズレ、将来バグの温床）

| 項目 | config.py 側 | 実際に使われている値 |
|---|---|---|
| `MJX_NUM_ENVS`/`BATCH_SIZE`/`UNROLL_LENGTH` | 2048 / 1024 / 20 | `train/train_mjx.py` のargparseデフォルトは 256 / 256 / 10（config.py側は完全に未参照＝死んだ設定） |
| `TOTAL_TRAINING_STEPS_ESTIMATE` | 25,000,000（USE_REFERENCE_GAIT=False時） | `train_mjx.py` のGPUデフォルト `steps=10_000_000`。カリキュラム進捗計算の分母が2系統存在 |
| `GAIT_STAND_HEIGHT` | 0.23m | 実際の直立姿勢高さ`INITIAL_HEIGHT`=0.1773m、報酬計算の`COM_HEIGHT`=0.17m。USE_REFERENCE_GAIT=Trueにした瞬間に矛盾が顕在化する |
| `NUM_JOINTS`/`BASE_OBS_DIM`/`OBS_DIM` | `len(JOINT_NAMES)`等から計算 | `real/real_env.py`のクラス変数として `20`/`84`/`625`が**独立にハードコード**（値は現状一致だが同期機構なし） |
| `mjx_env.py reset()`の `qpos.at[0:3].set([0,0,0.1773])` | `RobotConfig.INITIAL_HEIGHT`は使わず直値0.1773をハードコード | 同様に `scratch/gate0_formal_eval.py` 等でも`0.1773`が複数箇所に直書き |

## 🟢 軽微（未使用・重複定義。バグではないが要整理）

- **`ARM_SWING_LIMIT_DEG` / `ALLOW_ARM_SWING`**（config.py）… `mjx_env.py`/`mjx_rewards.py`のどこからも参照されていない
- **`NOISE_ANGULAR_POS` / `NOISE_ANGULAR_VEL` / `NOISE_IMU_ANGLE` / `NOISE_IMU_GYRO`**（config.py）… 実際に使われているのは`NOISE_BASE_POS`/`NOISE_LIN_VEL`のみ
- **`REWARD_WEIGHTS["symmetry"]`**… 重み0.0のまま、report計算に一切登場しない
- **`FSR_CONTACT_THRESHOLD`(=0.5) vs `FOOT_CONTACT_THRESHOLD`(=0.05)**… 似た名前の閾値が2つ存在し、実際に使われているのは後者のみ
- **`PRIVILEGED_OBS_DIM`/`privileged_obs`**… `reset()`で計算・`info`に格納されるが、最終観測ベクトル(`final_obs`)には一切含まれない＝RMA廃止時の名残りの死にコード
- **`assets/humanoid.xml`（トップレベル）**… `RobotConfig.MUJOCO_MODEL_PATH`が指すのは`assets/humanoid/humanoid.xml`なので、トップレベル版は使われていない重複ファイル（中身も微妙にrgba属性の有無が違う）
- **`self.latency_steps`**（mjx_env.py）… コンストラクタで受け取り保存されるが、`step()`内では別ロジック（`jax.random.randint`による独自遅延）が使われており未参照

---
最も直すべきなのは**①のmetrics pytree不整合**（潜在的にjax.lax.scan/vmapでの実行時エラーや誤集計につながる）と、**②のKP/KD不一致**（意図した剛性が実は学習・実機どちらにも反映されていない）です。どれか特定の箇所から着手しますか？(改良規約上、1回の変更は1カテゴリに絞るのが良さそうです)
さらに深く掘りました。前回見落としていた、かなり重大な不整合が2つ見つかりました。

## 🔴🔴 最重大（新規発見）

### A. `data.sensordata[:8]` が実は足裏センサーではなくIMUデータを拾っている

`humanoid.xml` の `<sensor>` ブロックの宣言順は：
```xml
<gyro name="torso_gyro" .../>            <!-- index 0-2 (3値) -->
<accelerometer name="torso_accel" .../>  <!-- index 3-5 (3値) -->
<framequat name="torso_quat" .../>       <!-- index 6-9 (4値) -->
<touch name="sensor_l_foot_fl" .../>     <!-- index 10 -->
<touch name="sensor_l_foot_fr" .../>     <!-- index 11 -->
... (計8個のtouchセンサーは index 10〜17) -->
```
MuJoCoのsensordataは宣言順にフラット結合されるので、touch(FSR)センサーは **index 10〜17** です。

ところが `envs/mjx_env.py` の `_get_obs()`：
```python
if nsensor >= 8:
    fsr_data = data.sensordata[:8]   # ← index 0〜7 を取得
```
これは実際には `[gyro_x, gyro_y, gyro_z, accel_x, accel_y, accel_z, quat_w, quat_x]` であり、**FSRタッチ値は一つも含まれていません**。

さらに `envs/mjx_rewards.py` の `compute()`：
```python
left_foot_force = jp.where(has_sensors, jp.clip(jp.mean(jp.abs(data.sensordata[0:4] + 1e-6)), 0.0, 100.0), 0.5)
right_foot_force = jp.where(has_sensors, jp.clip(jp.mean(jp.abs(data.sensordata[4:8] + 1e-6)), 0.0, 100.0), 0.5)
```
同じバグで、`left_foot_force`/`right_foot_force` は実際には `[gyro×3, accel×1]` と `[accel×2, quat×2]` の平均絶対値です。これが波及する範囲：
- 観測ベクトルの `fsr_data`（8次元）と、そこから計算される `zmp`（2次元）が丸ごとノイズ
- `both_feet_contact` 報酬（重み8.0）
- `stability_metrics.py` に渡る `left_foot_force`/`right_foot_force` → `foot_balance`, `zmp_margin`, `stability_index` 全部
- `swing_is_left`（Capture Point計算のswing/stance判定）
- disturbance recovery bonusのゲーティング条件

`docs/current.md` には「シミュレーション内のFSR連続値・ZMP指標は学習／評価専用」と書かれており、これは「連続値だが実際の接触力を表す」ことを前提にした記述なので、設計意図とも矛盾しています。既存の単体テスト（`test_improved_rewards.py`等）は全て手動構築したダミー入力を使っており、実際のXML＋MJXパイプラインを通していないため、このバグを検出できていません。

### B. `joint_pos`/`joint_vel` の並び順がシミュレーションと実機でズレている（ABI破壊）

`humanoid.xml` の `<worldbody>` 内のbody入れ子順序は：
```
左腕(4関節) → 右腕(4関節) → 左脚(6関節) → 右脚(6関節)
```
なのでMuJoCoの `data.qpos[7:]` / `data.qvel[6:]` は**このツリー順**で並びます。

一方 `RobotConfig.JOINT_NAMES` / `DEFAULT_JOINT_ANGLES`、`<actuator>` ブロック、そして `real/real_env.py` の `joint_pos = self.smoothed_action`（actuator順で計算される）は：
```
右脚(6) → 左脚(6) → 右腕(4) → 左腕(4)
```
という**全く別の並び順**です。

`envs/mjx_env.py::_get_obs()`：
```python
joint_pos = data.qpos[7:]   # ツリー順: 左腕,右腕,左脚,右脚
joint_vel = data.qvel[6:]   # 同上
```
これがそのまま観測ベクトル(`base_obs`のindex 12〜52)に入ります。つまり**学習時の観測「joint_pos」と実機の観測「joint_pos」は、同じインデックス位置に全く別の関節の値が入っている**状態です。学習済みポリシーを実機に載せた瞬間、関節角情報が完全にシャッフルされた状態で入力されることになり、`caveat.md`が明記する「関節角・関節速度の順番」ABI契約に違反します。

興味深いのは、**同じ`mjx_env.py`の`reset()`内では正しく処理されている**点です：
```python
jnt_id = sys_mj_model.actuator_trnid[act_i][0]
qpos_adr = sys_mj_model.jnt_qposadr[jnt_id]
actuator_to_qpos_list.append(qpos_adr)   # actuator順→qposアドレスへの正しい変換
```
初期姿勢設定(`reset()`)はこの`_actuator_to_qpos_idx`でactuator順→qpos順に正しく変換しているのに、観測取得(`_get_obs()`)では同じ変換をせず生の`qpos[7:]`を使っています。**同一ファイル内で処理の一貫性が取れていない**ことが、これがバグである強い状況証拠です。

---

## 🟡 中程度（新規発見）

3. **サンプルされるが一度も消費されないドメインランダム化変数が3つ**
   - `info['dr_kp_scale']`（reset()で`0.7〜1.3`をサンプル）… step()内で一切参照されず、アクチュエータ剛性のランダム化は実質機能していない
   - `info['disturbance_scale']`（reset()で`0.0〜1.0`をサンプル）… step()内で未参照
   - `info['action_buffer']`（reset()でゼロ初期化）… step()内で未参照

4. **`MJXRewardSystem.compute()`の引数`reference_action`が関数内で一度も使われていない**（デッドパラメータ）。`mjx_env.py`は`info['reference_action']`を毎ステップ計算してこの引数に渡しているが無意味。

5. **`PUSH_DIRECTIONS=8` / `PUSH_DURATION_STEPS=1` は学習時の実際の外乱生成ロジックに全く反映されていない**
   - 実際の方向サンプリングは離散8方位ではなく**連続一様分布**（しかも`minval=[-0.5,-1.0,-0.2], maxval=[0.5,1.0,0.2]`という非立方体の箱から正規化しているため方向分布がY軸に偏る可能性あり）
   - 「持続時間」は`PUSH_DURATION_STEPS`を参照せず、毎ステップ独立の`p=0.03`（コード内マジックナンバー、config.py未定義）のベルヌーイ試行
   - `scratch/phase0_eval_diagnostics.py`の出力JSONが自己矛盾：`"directions": 8`と`"implementation": "...direction is sampled continuously"`を同じレポート内に両方出力している

6. **カリキュラムスケール計算が2ファイルに完全重複**：`mjx_env.py::_get_curriculum_scale()`と`mjx_rewards.py::_get_curriculum_disturbance_scale()`は実装が一字一句同じ。前者は実際の外力に使われ、後者はログ用metricsにしか使われていない（今は結果一致するが、将来どちらかだけ編集されるとズレる）。

7. **USE_REFERENCE_GAIT=Trueにした場合の潜在的な初期姿勢の不連続**：`reset()`はフラグに関わらず常に`DEFAULT_JOINT_ANGLES`（中腰姿勢）で初期化するが、`step()`の残差目標は1ステップ目から`jax_get_reference_trajectory(phase=0,...)`基準に切り替わる。両者が同じ姿勢である保証はコード上ない。

---

## 🟢 軽微（新規／再確認）
- `self._actuator_indices`（mjx_env.py `__init__`）… 計算されるがクラス内どこからも参照されない
- `RobotConfig.REWARD_WEIGHTS['symmetry']`… 全`w[...]`アクセス箇所を洗い出して確認、確定で未使用
- `RobotConfig.FSR_CONTACT_THRESHOLD`(0.5)… 実際に使われているのは別名の`FOOT_CONTACT_THRESHOLD`(0.05)のみ（前回指摘済みの再確認）

---
特にA・Bは実際の学習品質・sim-to-real移行に直結する重大バグの可能性が高いです。さらに続けて `real_io.py` の通信プロトコル周りや XML のメッシュ/慣性値まで掘り下げますか？それとも一旦ここまでの報告で区切りますか？
さらに掘りました。前回発見した「関節順序ミスマッチ（バグB）」の波及範囲と、FSR/センサー座標系まわりで新たな不整合が見つかりました。

## 🔴🔴 新規の重大発見

### C. `real_io.py` の±120度ハードクランプが `config.py` の関節可動域と矛盾している
`real/real_io.py` の `BusLinkerV3.sync_write_positions()`：
```python
angle_deg = np.clip(angle_deg, -120.0, 120.0)  # ±120度ハードクランプ
pos = int(np.clip((angle_deg + 120.0) / 240.0 * 1000.0, 0, 1000))
```
これは意図的なコメント付きで「±120度」に固定クランプしています（Hiwonderシリアルサーボの一般的な総回転角240度に対応する、物理的な仕様上の制約と思われます）。

ところが `robot/config.py` / `humanoid.xml` の `JOINT_LIMITS` は、これを超える可動域を許容している関節が複数あります：
- `right_hip_yaw`: `[0°, 180°]`
- `left_hip_yaw`: `[-180°, 0°]`
- `right_shoulder_roll` / `left_shoulder_roll`: `[-180°, 180°]`
- `right_shoulder_pitch` / `left_shoulder_pitch`: `[0°, 180°]` / `[-180°, 0°]`
- `right_elbow` / `left_elbow`: `[0°, 180°]` / `[-180°, 0°]`

さらに変換式 `(angle_deg + 120.0) / 240.0 * 1000.0` は「0ラジアン＝サーボ中央（pos=500）」を前提にしていますが、上記の非対称な関節（例：`right_hip_yaw`は`[0°,180°]`で0ラジアンが可動域の**端**）ではこの前提が成立しません。**シミュレーションで学習した方策がこれらの関節に大きな角度を指令しても、実機では120度でクランプされ、しかも変換式自体が関節ごとのオフセットを考慮していない**ため、sim-to-realで挙動が大きくズレる可能性が高いです。

### D. `RobotConfig.FSR_POSITIONS` の座標が実際のXML内センサー配置と一致しない
`robot/config.py`:
```python
FSR_POSITIONS = np.array([
    [-0.08, -0.04], [0.08, -0.04], [-0.08, 0.04], [0.08, 0.04],  # Right foot
    [-0.08,  0.04], [0.08,  0.04], [-0.08, -0.04], [0.08, -0.04],  # Left foot
])
```
一方、`humanoid.xml` に実際に定義されているFSRサイト座標：
```xml
<site name="l_foot_fsr_fl" pos="0.012 0.027 -0.0541" .../>
<site name="l_foot_fsr_fr" pos="-0.012 0.027 -0.0541" .../>
<site name="l_foot_fsr_bl" pos="0.012 -0.070 -0.0541" .../>
<site name="l_foot_fsr_br" pos="-0.012 -0.070 -0.0541" .../>
```
スケールも(±0.08m vs ±0.012m)、非対称なy範囲(-0.070〜0.027)も全く一致していません。さらに `mjx_env.py::_get_obs()` のZMP計算：
```python
foot_positions = jp.array(RobotConfig.FSR_POSITIONS)
zmp_x = jp.sum(foot_positions[:, 0] * fsr_data) / total_p
```
は、左右の足を**同じローカル座標系**として扱っており、実際の足のワールド座標オフセット（両足の間隔）を一切考慮していません。左右の足がどれだけ離れていても計算結果が変わらない設計です（※もっとも、`fsr_data`自体が前回発見のバグAで既に汚染されているため、この座標ズレは「二重に壊れている」状態です）。

### E. Domain Randomizationの減衰・摩擦係数がactuator順で生成されるのに、joint velocityはツリー順に適用されている（バグBの波及）
前回報告した「`joint_pos`/`joint_vel`の並び順がactuator順とXMLツリー順でズレている」バグの追加の影響範囲です。`mjx_env.py::step()`：
```python
'dr_damping': jax.random.uniform(key_dr1, shape=(self._mjx_model.nu,), ...),  # ← actuator順で生成
'dr_friction': jax.random.uniform(key_dr2, shape=(self._mjx_model.nu,), ...),
...
joint_vel = state.pipeline_state.qvel[6:]  # ← ツリー順（左腕,右腕,左脚,右脚）
damping_torque = -info['dr_damping'] * joint_vel
friction_torque = -info['dr_friction'] * jp.sign(joint_vel)
qfrc_applied = qfrc_applied.at[6:].add(damping_torque + friction_torque)
```
`dr_damping[0]`は本来「right_hip_yaw用の減衰係数」のつもりで生成されていますが、実際には`qvel[6]`（ツリー順で最初に来る左腕の関節）に適用されてしまいます。ドメインランダム化が意図した関節と全く違う関節にかかっているため、このDRが「関節ごとの個体差を模倣する」という目的を果たせていません。

---

## 🟡 その他、確認して見つけた軽微な点

- `envs/mjx_rewards.py::_compute_disturbance_recovery_bonus()` の`getattr(RobotConfig, 'RECOVERY_BONUS_STABILITY_THRESHOLD', 0.7)` — フォールバックのデフォルト値(0.7)が、実際に`config.py`で設定されている値(0.4)と食い違っている（実害はないが、設定が消えた場合に古い値へ静かにフォールバックする）
- `scripts/visualize_mujoco.py` / `scratch/render_collision.py` はXMLロード後に `model.opt.timestep` を`RobotConfig.SIM_DT`で上書きしていない（`scratch/gate0_mujoco_eval.py`は上書きしている）ため、XML内の`timestep="0.002"`のまま動く＝学習時のタイムステップ(0.0025)と可視化時のタイムステップが異なる
- `gate0_formal_eval.py` と `gate0_mujoco_eval.py` は同じ「Gate 0」判定なのに合格基準の項目セットが微妙に異なる（`foot_touch_rate`基準 vs `both_foot_contact_rate`+`torque_saturation_rate`基準）

---

特に **C（±120度クランプと関節可動域の矛盾）** と **D/E（FSR座標・DR適用先のミスマッチ）** は、前回報告したA・Bと合わせると「実機投入時に足回り・上半身の広可動域関節でシミュレーションと全く違う動きになる」「外乱への個体差ランダム化が事実上機能していない」という、実機移行前に必ず潰しておくべき問題群になっています。

さらに掘りますか？（メッシュ慣性値の物理的妥当性や、`gait_generator.py`/`kinematics.py`側—現状`USE_REFERENCE_GAIT=False`で未使用だが将来有効化した場合の整合性—はまだ精査していません）
GAIT_THIGH_LEN/GAIT_KNEE_LEN の実寸との乖離を確認できました。これがどう波及するか、実際に歩容生成コードを動かして検証します。さらに検証しました。これは前回の「STAND_HEIGHTが最大リーチを超過している」という私の予想より、実は**もっと根が深いバグ**でした。GAIT_STAND_HEIGHT のデッドパラメータバグを実証できました。これで `gait_generator.py`/`kinematics.py` 周りの全体像がはっきりしました。まとめます。

## 🔴🔴 新規の最重大発見

### F. `GAIT_STAND_HEIGHT` が計算に一切使われていないデッドパラメータ（実証済み）
`robot/gait_generator.py` の `jax_cycloid_trajectory(phase, foot_height, step_length, stand_height)`：
```python
def jax_cycloid_trajectory(phase, foot_height, step_length, stand_height):
    ...
    z_swing = (foot_height / np.pi) * (1.0 - jp.cos(theta_swing))
    ...
    z_traj = jp.where(is_swing, z_swing, z_stance)
    return x_traj, z_traj
    # stand_height を使う行がソースコード上のどこにも存在しない
```
`stand_height`（呼び出し元では `RobotConfig.GAIT_STAND_HEIGHT=0.23`）を実際に `0.23` と `999.0` に差し替えて実行し、**出力が完全に同一になる**ことを実証しました。つまり「腰の高さまで脚を伸ばす」という歩容の根幹部分が実装から欠落しており、IKに渡される目標z座標は常に `[0, foot_height=0.04]` という極小レンジのみです。同じロジックが `GaitGenerator.get_foot_position()`（NumPy版、`real_env.py`が実機で使う経路）にも存在し、`self.stand_height`は保存されるだけで一度も参照されません。**シミュレーション側・実機側どちらの参照歩容も、同じ根本原因で「膝がほとんど曲がらない、腰高もゼロの棒立ち軌道」になります。**（現在は`USE_REFERENCE_GAIT=False`のため発現していない潜伏バグ）

### G. `GAIT_THIGH_LEN`/`GAIT_KNEE_LEN`（各0.12m）が実機XMLのリンク長と約43%乖離
左脚の関節位置をXMLからそのまま順運動学で解いた結果：
```
THIGH長 (hip_pitch → knee)   = 68.70 mm   ← config.py想定は 120.00 mm
SHIN長  (knee → ankle_pitch) = 68.70 mm   ← config.py想定は 120.00 mm
```
`kinematics.py`の`LegKinematics.L_THIGH/L_SHIN`も同じ`RobotConfig.GAIT_THIGH_LEN/GAIT_KNEE_LEN`を参照しているため、実機/CADの実寸を全く反映していません。仮にF（デッドパラメータ）を直しても、この値のままではIKの解が実際のロボット形状と一致しません。

さらに副産物として、`GAIT_STAND_HEIGHT=0.23m`は**config.py上の理論最大リーチ(0.12+0.12=0.24m)にすら収まらず**、実測リーチ(0.1374m)の約1.7倍を要求する値になっています。Fのバグで実害は隠れていますが、Fだけ直すと即座にこちらの限界超過が表面化する「二重の地雷」構造です。

## 🟡 追加の中程度所見（既存の指摘との関連）

### H. `ACTION_SCALE`(±30°)は多数の関節でCBF介入が常態化する設計になっている
`DEFAULT_JOINT_ANGLES`基準で`action=±1.0`を与えたときの飽和状況を全関節で計算：
```
right_hip_yaw / left_hip_yaw / right_ankle_roll / left_ankle_roll
right_knee / left_knee / right_shoulder_pitch / left_shoulder_pitch
right_elbow / left_elbow / right_wrist_pitch / left_wrist_pitch  → いずれも★飽和
```
20関節中12関節が、ごく普通の大きさの行動（|action|=1.0）で可動域を超えます。これ自体はCBFが対処する設計かもしれませんが、前回報告した「Action Distortion」監査（`action_saturation_max=0.5`＝平均飽和率50%超で警告）と組み合わせると、**この閾値は関節配置上ほぼ常時トリガーされる可能性が高く**、実際の方策の異常検知として機能しない（常時アラート＝実質無効化）懸念があります。

---

以上でひとまず主要ファイル（config.py, mjx_env.py, mjx_rewards.py, stability_metrics.py, safety/cbf.py, real_env.py, real_io.py, gait_generator.py, kinematics.py, humanoid.xml, training_wrapper.py, train_mjx.py）は一通り相互突き合わせが完了しました。残っている未精査領域は、`deploy/export_onnx.py`（ONNX変換パイプライン）と、各STLメッシュの慣性値(`<inertial mass=... fullinertia=...>`)の物理的妥当性くらいです。そちらも掘りますか？
実証されました。これは全ボディに影響する系統的な質量二重計上バグです。type="mesh"でも同様に発生するか、念のため実メッシュでも検証します。実証されました。これは前回までの発見を超える、**全ボディに影響する系統的な物理バグ**です。加えて、コードだけから確認できる重大な不整合も1つ見つけました。

## 🔴🔴🔴 最重大（新規発見・実機実証済み）

### I. `inertiafromgeom="true"` により、全身の質量が二重計上されている

`humanoid.xml` は `<compiler ... inertiafromgeom="true"/>` を使っており、各ボディに**2つのgeom**が定義されています：
```xml
<geom name="..._geom" type="mesh" mesh="..." contype="0" conaffinity="0" group="1"/>          <!-- 視覚用、mass属性なし -->
<geom name="..._collision" type="box/sphere/capsule" ... mass="0.6915" .../>                   <!-- 当たり判定用、CAD由来の実質量を明示指定 -->
```
`mass="0.6915"`（胴体の場合）は、`assets/all/all.xml`の`<inertial mass="0.6915349018002279"...>`（元のCAD由来の慣性データ）と一致する値であり、明らかに「このボディの正しい物理質量」として意図的に設定されています。

しかし `inertiafromgeom="true"` の下では、MuJoCoは**同一ボディに属する全geomの質量を加算**します。`contype="0" conaffinity="0"`は衝突判定を無効にするだけで、質量計算からは除外されません。視覚用meshジオムは`mass`/`density`属性を持たないため、**デフォルト密度1000kg/m³ × メッシュ体積**で質量が計算され、これが collision ジオムの明示質量に**加算**されます。

実際に検証用のバイナリSTL（体積0.1×0.1×0.1m³=0.001m³ → 密度1000で1.0kg相当）+ 質量0.6915kgのcollisionジオムを持つボディをMuJoCoに読み込ませたところ：
```
mesh(視覚)の体積由来の期待質量 = 1.000000 kg
collisionに明示したmass        = 0.691500 kg
MuJoCoが実際に計算したbody_mass = 1.691500 kg   ← 単純合算そのもの
```
`type="mesh"`でも通常のプリミティブでも全く同じ挙動でした。これは`humanoid.xml`内の**全20+ボディ**に当てはまるパターンなので、**シミュレーション上のロボットは意図した質量より大幅に重く（各ボディのメッシュ体積次第で数十〜100%以上過大に）なっている可能性が高い**です。これは：
- トルク要求・PD制御ゲイン(KP/KD)の妥当性
- 転倒判定・重心力学・外乱応答の全て
- `scratch/probe_physical_limits.py`が算出する`total_mass_kg`（Phase-1の物理限界計算の前提値）

に直接波及する、これまでで最も根本的な物理モデル不整合です。修正するには、視覚用meshジオムに`density="0"`（または`mass="0"`）を明示するだけで済みますが、現状は全ボディが未対応です。

## 🔴 重大（新規発見・コード読解で確定）

### J. 学習時と評価時でポリシー分布のクリッピングが一致していない

`train/train_mjx.py` はモジュールレベル（importされた瞬間）で以下を実行します：
```python
def _install_policy_std_cap():
    original_create_dist = brax_distribution.NormalTanhDistribution.create_dist
    def clipped_create_dist(self, parameters):
        ...
        loc = POLICY_MEAN_CLIP_SCALE * (loc / (1.0 + jnp.abs(loc)))
        scale = jnp.clip(scale, POLICY_MIN_STD, POLICY_MAX_STD)
        return brax_distribution._NormalDistribution(loc=loc, scale=scale)
    brax_distribution.NormalTanhDistribution.create_dist = clipped_create_dist
    ...
_install_policy_std_cap()   # ← import直後にグローバルパッチが適用される
```
これは**Braxライブラリのクラスメソッドをプロセス全体で書き換えるグローバルパッチ**で、`train.train_mjx`を一度でもimportしたプロセスでのみ有効になります。

一方 `train/visualize_rl.py` は**独自の別実装**の `make_policy_network_factory()` を持っており（`mean_clip_scale`引数すら渡していない）、かつ `train.train_mjx` を一切importしません：
```python
def make_policy_network_factory(observation_size, action_size, ...):
    return ppo_networks.make_ppo_networks(
        ...
        # mean_clip_scale が渡されていない
    )
```

`scratch/gate0_formal_eval.py` と `scratch/phase0_eval_diagnostics.py` はどちらも `train.visualize_rl` からしか import しません（`train.train_mjx`は一切importしていない）。つまり：

| 実行経路 | `train.train_mjx`をimportするか | ポリシー分布のクリッピング |
|---|---|---|
| 学習本体 (`train_mjx.py`) | ✅ | あり (std∈[0.15,3.0], loc soft-clip±3.0) |
| ONNX変換 (`deploy/export_onnx.py`) | ✅（train_mjx由来の関数を直接import） | あり |
| 軌跡エクスポート (`train/export_trajectory.py`) | ✅ | あり |
| **Gate 0正式評価** (`scratch/gate0_formal_eval.py`) | ❌ | **なし（素のBrax挙動）** |
| **Phase 0診断評価** (`scratch/phase0_eval_diagnostics.py`) | ❌ | **なし（素のBrax挙動）** |
| インタラクティブ再生 (`play_mjx.py`→`visualize_rl.py`) | ❌ | **なし** |

**このプロジェクトのGate 0/Phase 0合否判定に使われている評価スクリプトそのものが、学習時とは異なる（クリップされていない）方策分布で推論している**ことになります。deterministic評価であっても`loc`のsoft-clip変換自体が異なるため、平均行動の値も変わります。これは`docs/status.md`が繰り返し言及している「評価時に学習時の性能が再現しない」という既知の症状（KLスパイク・episode_alive低下の切り分け対象）の**説明されていない一因**になっている可能性があります。

## 🟡 軽微（新規）

### K. ロックされた依存バージョンが2つのファイルで矛盾している
- `requirements-lock.txt`（リポジトリルート）: `jax==0.11.0`, `brax==0.14.2`
- `tmp_pip_show.txt`（同じくリポジトリルート）: `jax Version: 0.4.21`, `brax Version: 0.12.5`, `Location: C:\bipedal_robot\venv\...`（WSL移行前のWindowsネイティブvenvを指している）

後者はファイル名からして`pip show`の出力を一時保存した残骸ファイルと思われ、明らかに古い環境（Windows実行時代）の情報です。実害はほぼありませんが、どちらが「正」の依存バージョンか一見して分からず、紛らわしい重複ファイルです。

---

**I（質量二重計上）は、これまでの全発見の中で最も影響範囲が広く根本的な問題です。**なぜなら、A（センサーindexズレ）やB（関節順序ズレ）が「観測が壊れている」レベルなのに対し、Iは「物理シミュレーション自体の質量分布が壊れている」レベルであり、この上に築かれた学習結果・報酬設計・外乱耐性の議論すべての前提を揺るがすためです。

続けて掘るとすれば、残りは「実際の`_collision`ジオムの`pos`/`size`が対応する関節位置と幾何学的に整合しているか」（`assets/fix_collision_geoms.py`のロジック検証）くらいですが、これも実施しますか？
「渡したもの以外」ということで、外部の一次情報（実際のハードウェア仕様・ライブラリの実仕様）と照合してみます。「渡したもの以外」＝外部の一次情報（実ハードウェア仕様・実ライブラリの公式情報）と照合した結果、2種類の収穫がありました。

## ✅ 検証の結果「バグではなかった」項目（念のため確認）
- **BNO055のレジスタアドレス**（`real_io.py`の`REG_QUA_DATA_W_LSB=0x20`, `REG_GYR_DATA_X_LSB=0x14`, `REG_LIA_DATA_X_LSB=0x28`, `NDOF_MODE=0x0C`）は、Bosch公式データシート・Adafruit公式ドライバと完全に一致。正しい実装でした。
- **Hiwonderシリアルバスサーボのプロトコル**（ヘッダ`0x55 0x55`、チェックサム`~(ID+Len+Cmd+Params)&0xFF`、`CMD_SERVO_MOVE_TIME_WRITE=0x01`）も公式ドキュメント通りで正しい実装でした。

## 🔴🔴 新規の重大発見（外部情報との照合で判明）

### L. アーキテクチャ全体の土台にしている `brax.envs` が、pinされているバージョンの公式ドキュメントで**非推奨と明記されている**
PyPI公式ページ（brax 0.14.2、まさに`requirements-lock.txt`で固定されているバージョン）の説明文：
> **"WARNING Only brax/training is actively being maintained as of 0.13.0. Instead of brax/envs, users should use MuJoCo Playground... please use MJX available at github.com/google-deepmind/mujoco... rather than Brax as a wrapper to MuJoCo physics simulation."**

つまりBrax開発元自身が「`brax/envs`はもうメンテナンスされていない。MJXを直接使うかMuJoCo Playgroundを使え」と明言しています。ところが、このプロジェクトの環境実装 `envs/mjx_env.py::SenpuuMaruMJXEnv` は丸ごと：
```python
from brax.envs.base import PipelineEnv, State
class SenpuuMaruMJXEnv(PipelineEnv):
```
という、**まさに非推奨と名指しされているクラス**を継承しています。これは`train/train_mjx.py`にある大量のモンキーパッチ群（`_safe_unpmap`、`jax.device_put_replicated`の互換シム、`ppo_networks.make_inference_fn`の差し替え、`NormalTanhDistribution.create_dist`のパッチ）が**なぜ大量に必要になっているのか**を説明します——これらは全て、「非推奨扱いのAPI」を「非推奨化後もリリースされ続けている最新のjax/brax」に無理やり適合させるための延命措置です。プロジェクトの設計選択そのものが、pinしている依存バージョンの公式な推奨パスと矛盾している、という根の深い不整合です。

### M. `jax==0.11.0` は実在するが `Python >= 3.12` を要求する（PyPI公式確認済み）
`requirements-lock.txt`に`jax==0.11.0`（2026年7月16日リリース、実在確認済み）とありますが、PyPI上の分類子は明確に：
```
Requires: Python >=3.12
Programming Language: Python :: 3.12, 3.13, 3.14, 3.15
```
このプロジェクトのどこにも実行環境のPythonバージョンを明示的に固定する記述がなく（`real/setup/install_rt.sh`は`python3-venv`をapt installするのみでバージョン指定なし）、もし実行環境（WSL venv、あるいはRaspberry Pi実機のRT-Preemptセットアップ）がPython 3.11以下であれば、`pip install -r requirements-lock.txt`自体が失敗します。`tmp_pip_show.txt`が示す旧環境（`jax Version: 0.4.21`, `Location: C:\bipedal_robot\venv\...`）は明らかにこの制約が緩い古いjaxバージョンでの実行環境であり、WSL移行後に`jax==0.11.0`へジャンプした際、Pythonバージョン要件が変わったことに気づかず`requirements-lock.txt`だけ更新した可能性を示唆します。

---

**外部の一次情報と照合するアプローチ自体が、プロジェクト内部の記述だけでは見えなかった「設計選択そのものの妥当性」という新しい層のバグを掘り当てました。** L（brax.envs非推奨）は今までの発見（A〜K）とは種類が違い、「個別のコードのミス」ではなく「土台となるライブラリの使い方そのものが公式ガイダンスと矛盾している」というアーキテクチャレベルの指摘です。

さらに外部照合を続けるなら、MuJoCo 3.11.0の実在性・`mujoco-mjx`のAPI変更履歴（特に`data.subtree_com`や`actuator_force`のフィールド仕様が使用バージョンで変わっていないか）を確認できますが、いかがしますか？
HX-30HM実機の**公式スペック**（Hiwonder公式サイト・正規販売店）を確認したところ、想像以上に大きな不整合が見つかりました。

## 🔴🔴🔴 新規の最重大発見（外部の公式スペックと直接矛盾）

### N. `real_io.py` のサーボ位置変換式が、実際に使っているサーボの protocol と根本的に違う

Hiwonder公式ページ・公式BusLinker V3.0マニュアルで確認した **HX-30HM/HX-35HM（同じ磁気エンコーダー系サーボ）の実仕様**：
> 「12-bit high precision magnetic encoder... **can extend its angle to 360 degrees**」
> 「**0°-360°rotation angle corresponds to the value of 0-1500** in servo PC software」

つまり実機のHX-30HMは：
- 可動域 **0〜360度**（磁気エンコーダー化で、旧来のポテンショメータ式サーボの240度制限を撤廃したのがこの型番の最大の特徴）
- コマンド値レンジ **0〜1500**

ところが `real/real_io.py` の変換式：
```python
angle_deg = np.clip(angle_deg, -120.0, 120.0)  # ±120度=合計240度を想定
pos = int(np.clip((angle_deg + 120.0) / 240.0 * 1000.0, 0, 1000))  # 0-1000を想定
```
は、**旧世代のLX-16A（ポテンショメータ式、0-1000が0-240度に対応）の規格をそのまま流用**しています。`docs/old/hardware.md`にも明記されている通り、このプロジェクトが実際に採用しているのはHX-30HM（磁気エンコーダー式、360度対応）であり、上記の変換式とは**プロトコルの世代が丸ごと違います**。

具体的な実害：
1. `np.clip(pos, 0, 1000)` という**ハードな上限**により、実際のサーボが物理的に対応している`1000〜1500`（＝実角度240°〜360°の範囲）に**絶対にコマンドを送れない**
2. `right_hip_yaw`(可動域`[0°,180°]`)や`right_shoulder_roll`(可動域`[-180°,180°]`)のような広可動域関節は、シミュレーション上は問題なく動いても、実機では「pos=1000でクリップされた位置」という**意図と全く異なる角度**に固定されてしまう
3. 前回報告した「C（±120度クランプと関節可動域の矛盾）」は、実は「サーボの物理的な限界に対する誤解」ではなく、**「そもそも別サーボ型番のプロトコル定数を丸ごと誤用している」**という、より根本的な原因だったことが判明しました

### O. `MOTOR_MAX_VELOCITY` の値がコメントに書かれた仕様値と一致しない
`robot/config.py`:
```python
MOTOR_MAX_VELOCITY = 6.5     # [rad/s] (0.19sec/60deg @11.1V)
```
コメントが引用する仕様「0.19秒で60度」を実際に計算すると：
```
60° ÷ 0.19秒 = 315.8°/s = 5.51 rad/s
```
設定されている値は**6.5 rad/s**であり、コメントが引用する仕様値から計算される**5.51 rad/sとは約18%の乖離**があります（6.5 rad/sに対応する実際の仕様は「0.16秒で60度」に相当）。この値は`mjx_env.py::step()`の`max_delta = RobotConfig.MOTOR_MAX_VELOCITY * RobotConfig.CONTROL_DT`というレートリミット計算に直接使われるため、シミュレーション上の関節速度上限が実機より最大18%甘く(速く)設定されている可能性があります。

---

**N（サーボプロトコル世代の誤用）は、実機投入前に絶対に潰す必要がある種類のバグです。** これまで発見してきた関節順序ズレ（B）、可動域クランプの矛盾（C, 精緻化）と組み合わさると、「実機に載せた瞬間、複数の広可動域関節が本来の可動域の一部にしかアクセスできず、しかもクリップされた位置がシミュレーションの想定と全く違う」という、sim-to-real移行を根本から妨げる問題群になっています。

さらに掘るとすれば、`data.sensordata`のインデックス問題（バグA）と絡めて、Teensyファームウェア側のFSR ADC閾値判定ロジック（このリポジトリにはPythonコードのみで、実際のTeensyファームウェアのCソースは含まれていない）との整合性、あるいは`mujoco==3.11.0`のAPI変更履歴を確認できますが、いかがしますか？
さらに掘ったところ、これまでの発見の一部について**重要な訂正**をしなければならない、メタ的な発見に行き着きました。

## ⚠️ 前回までの発見(C, N)に対する重要な訂正

### P. `BusLinkerV3` / `BNO055UART` は実行時に一度も使われていないデッドコードだった

`real/real_env.py`（実際に実機で動くメインループ）を再確認すると：
```python
from real.real_io import TeensySpineIO
...
self.spine = TeensySpineIO(num_servos=self.NUM_JOINTS)
```
**`TeensySpineIO`しかimportしていません。** `real_io.py`内の`BNO055UART`クラス（約100行）と`BusLinkerV3`クラス（約150行、モジュール直下の`calc_checksum()`含む）は、リポジトリ全体を検索しても**どこからも一度もインスタンス化されていません**。

つまり：
- 実際の通信経路は `RaspberryPi5 --(USB, 73バイトテレメトリ)-- Teensy4.1 --(内部でUART×4本+BNO055)-- サーボ群/IMU` という**Teensy集中管理型アーキテクチャ**（`docs/current.md`が明記する「脳脊髄分離システム」）
- `BNO055UART`/`BusLinkerV3`が実装しているのは、**RaspberryPiが直接BNO055やBusLinkerとUARTで通信する旧世代アーキテクチャ**（`docs/old/hardware.md`が説明する「AE-LLCNV-LVCH16T245レベル変換で4系統のUART TX/RX」という構成そのもの）

**これは前回報告した C（±120度クランプの矛盾）と N（HX-30HMプロトコル世代の誤用）の重大性を訂正する必要があることを意味します。** これらは実在するコード上の不整合ですが、**現在実機を実際に動かしているコードパスには影響しません**（Teensy集中管理型に移行済みで、旧アーキテクチャのPythonコードは孤立した死にコードとして取り残されている状態）。実際の「radians→サーボ位置値」変換は、このリポジトリに含まれていないTeensyファームウェア（C/C++）側で行われているはずで、そちらの正しさはこのPythonリポジトリからは検証できません。

これ自体が新しい発見です：**docs/old/hardware.mdが説明するハードウェア構成（RPi直結UART×4+BNO055）と、実際に使われているコード（Teensy経由USB一本）が、ドキュメントとコードのレベルで完全に食い違っている**状態です。

## 🔴 新規の重大発見（実際に使われている経路にある）

### Q. 実際に使われている `TeensySpineIO` の73バイトテレメトリには、チェックサム/CRCが一切ない

`real_io.py::TeensySpineIO.communicate()`（こちらは実際に使われている）：
```python
raw = self.ser.read(73)
if len(raw) >= 73 and raw[0] == 0x5A:
    w, x, y, z = struct.unpack('<4f', raw[1:17])
    gx, gy, gz = struct.unpack('<3f', raw[17:29])
    ax, ay, az = struct.unpack('<3f', raw[29:41])
    fsr_contacts = ... struct.unpack('<8f', raw[41:73])
```
検証しているのは「開始バイト`0x5A`が正しいか」「長さが73バイトぴったりか」の2点のみで、**パケット内容の破損を検出するチェックサム/CRCが一切ありません**。クォータニオンだけは事後的にノルムチェック（`0.5〜1.5`の範囲か）で異常値をある程度弾けますが、**ジャイロ・加速度・FSR8chの値には一切の妥当性検証がありません**。

一方、前回報告した「REAL-4」修正（チェックサム検証の厳格化）は、実は**使われていない`BusLinkerV3`クラス**に対して行われたものでした。つまり、このプロジェクトが払ったチェックサム強化の労力は、**実際には動いていない経路に費やされ、本当に実機で動いている経路（USB 73バイトテレメトリ）は無防備なまま**という、労力配分のミスマッチが起きています。IMU由来のroll/pitchは転倒判定・報酬計算に直結するため、ビット化けした値がそのまま安全性判断に使われるリスクがあります。

---

**Pは「これまでの発見の信頼性そのものへの但し書き」として重要**です。C・Nは無意味になったわけではなく、「デッドコードとして削除すべき／もしくは旧設計に戻すなら直すべき」対象として位置づけ直す必要があります。一方でQは、実際に動いている経路そのものの新しい安全性ギャップです。

続けるなら、Teensyのファームウェア自体はこのリポジトリに含まれていないため、これ以上ハードウェア通信層を外部照合で掘るのは限界に近いです。代わりに`deploy/export_onnx.py`のONNXパイプライン（tf2onnx/jax2tfのバージョン互換性）か、`docs/`内の各種レポートで「修正済み」と書かれている箇所が実際に現在のコードに残っているかの再監査、どちらに進みますか？
`requirements-lock.txt` そのものを全パッケージ横断でコードの`import`文と突き合わせたところ、**実行環境がそもそも壊れている**レベルの決定的な不整合が見つかりました。`requirements-lock.txt`を全パッケージ横断でコード内`import`文と突き合わせた結果、**環境構築そのものが破綻する**バグが確定しました。

## 🔴🔴🔴 新規の最重大発見

### R. `requirements-lock.txt` に、コードが実際にimportしている4パッケージが欠落している

| 欠落パッケージ | 実際にimportしている箇所 | 実害 |
|---|---|---|
| `tensorflow` | `deploy/export_onnx.py` **モジュールレベルで無条件import** | `requirements-lock.txt`だけで環境構築すると、**このファイルはimportした瞬間に`ModuleNotFoundError`でクラッシュ**する |
| `tf2onnx` | `deploy/export_onnx.py::export_jax_to_onnx()`内 | ONNX変換の本体関数が呼ばれた瞬間にクラッシュ |
| `onnxruntime` | `real/real_env.py::PolicyRunner`（try/exceptでdummy fallback） | クラッシュはしないが、**実機が「常にゼロ行動を出力するダミーモード」で無言のまま起動してしまう**（警告print以外に気づく手段がない） |
| `pyserial`(`serial`) | `real/real_io.py`, `real/real_env.py`（同様にtry/exceptでdummy fallback） | 同上、サーボ・IMU通信が全て無効化されたまま気づかず起動する |

つまり、**このプロジェクトの最終ゴールである「学習済みポリシーをONNX化して実機に載せる」という一連の作業(`deploy/export_onnx.py`)は、`requirements-lock.txt`通りに環境構築した瞬間に実行不可能**です。さらに実機のメイン制御ループ(`real_env.py`)自体も、ロック済み依存関係だけでは`onnxruntime`と`pyserial`が入らず、**サイレントに「何もしないロボット」として起動する**という、気づきにくい形の実害を持っています。

---

## 総括：ここまでの監査で確定した問題の全体像

今回の一連の監査で発見した項目を、実害の重大度で並べ直します（今回新規のRを含む）：

**🔴🔴🔴 実行不能・機能しない（最優先で直すべき）**
- **R**: `requirements-lock.txt`に`tensorflow`/`tf2onnx`/`onnxruntime`/`pyserial`が欠落。ONNXデプロイパイプラインと実機ループが機能しない
- **I**: `inertiafromgeom="true"`により全ボディの質量が二重計上（視覚meshジオムに`density`/`mass`指定がない）。物理シミュレーションの土台が壊れている
- **A**: `data.sensordata[:8]`がFSRではなくIMU値を拾っている（sensor宣言順とコードのインデックス前提がズレ）
- **B**: `joint_pos`/`joint_vel`（`qpos[7:]`/`qvel[6:]`）がXMLツリー順、他の全て（JOINT_NAMES/actuator/real_env）がactuator順で、観測のABIが破壊されている
- **N**: 実機サーボがHX-30HM(360°, pos 0-1500)なのに、`real_io.py`の変換式は旧世代LX-16A(240°, pos 0-1000)のプロトコルを誤用（ただし**P**により、この経路自体が現在使われていないデッドコードと判明）

**🟡 意味論的・設計的矛盾**
- **1**: `reset()`と`step()`でmetrics辞書のキー集合が不一致（`reward_is_finite`欠落）
- **3**: `TERMINATION_HEIGHT`が絶対座標系のコメントのまま、実装は相対座標系
- **F/G**: `GAIT_STAND_HEIGHT`がデッドパラメータ、`GAIT_THIGH_LEN/KNEE_LEN`が実寸の約1.75倍
- **J**: 学習時とGate 0評価時でポリシー分布のクリッピングが不一致（train_mjxのグローバルパッチが評価スクリプトに波及しない）
- **L**: アーキテクチャの土台(`brax.envs`)がpin済みバージョンの公式ドキュメントで非推奨と明記
- **P/Q**: ドキュメント(`hardware.md`)が説明する構成と実際に使われているコード(`TeensySpineIO`)が食い違い、かつ実際に使われている経路(73バイトテレメトリ)にチェックサムがない

**🟢 死んだコード・DRY違反・軽微な数値ズレ**
- 2（KP/KD未反映）, 4-8（引数・DR変数の未消費）, C/D/E（Pにより無効化済みと判明）, H（ACTION_SCALE飽和）, K（依存バージョンファイルの重複）, M（Python 3.12要件）, O（MOTOR_MAX_VELOCITYの計算誤差）, その他多数の未使用設定値

---

正直に言うと、ここが**静的解析で発見できる範囲の実質的な限界**です。これ以上「完全にゼロ」を保証するには、このリポジトリに含まれていない要素へのアクセスが要ります：
- **Teensyファームウェア本体（C/C++）** — FSR閾値判定・E-stopロジックの正しさはこちらのPythonコードから検証不可能
- **実際のSTLバイナリメッシュ** — 慣性の物理的妥当性（体積・重心オフセット）は寸法の近似計算はできても厳密検証は不可
- **実GPU環境でのフル学習実行** — KLスパイクや報酬ハッキングの実挙動は、静的なコードレビューでは「起こりうる」までしか言えず「実際に起きているか」は実行しないと確定できない

以上が今回の監査で私が到達できた誠実な終着点です。R以降、これ以上の新しいカテゴリの不整合を追加で発掘するのは困難と判断します。修正は他のチャットに任せるとのことなので、この一覧を渡してください。
これまでの監査（A〜R、①〜⑧）と重複しない範囲で、コード同士を突き合わせてさらに見つかった不整合を報告します。特に **S** と **V** は、Phase 0/1 の学習・評価の前提そのものを揺るがす種類の問題です。

## 🔴🔴🔴 最重大（新規）

### S. `training_progress` が学習ループの外では0に固定されたまま — 評価スクリプトでは`soft_penalty`が常にゼロになる

`envs/mjx_env.py::reset()` は `training_progress: jp.array(0.0)` で初期化し、`step()`内には以下の更新ロジックがあります。

```python
if '_env_steps' not in state.info:
    info['training_progress'] = jp.clip(
        jp.asarray(env_steps, dtype=jp.float32) / float(max(RobotConfig.TOTAL_TRAINING_STEPS_ESTIMATE, 1)),
        0.0, 1.0,
    )
```

しかし `reset()` は必ず `info['_env_steps'] = jp.array(0, ...)` を設定するため、**`state.info`に`_env_steps`キーが存在しない状態は`reset()`直後を含めて一度も発生しません**。つまりこの`if`ブロックは構造的に到達不能なデッドコードで、`mjx_env.py`自身は`training_progress`を一切更新しません。

実際に`training_progress`を更新しているのは`envs/training_wrapper.py::TrainingProgressWrapper`だけです。ところが、このラッパーを経由しているのは`train/train_mjx.py`の学習ループ**のみ**で、`scratch/gate0_formal_eval.py`・`scratch/phase0_eval_diagnostics.py`・`train/visualize_rl.py`・`scratch/gate0_standing_eval.py`は全て`SenpuuMaruMJXEnv`を素の`jax.jit(env.reset)`/`jax.jit(env.step)`で直接叩いており、`TrainingProgressWrapper`を一切経由しません。

この結果、`envs/mjx_rewards.py::compute()`内の

```python
progress_scale = 1.0 if training_progress is None else jp.clip(training_progress, 0.0, 1.0)
penalty_scale = intra_ep_scale * progress_scale
...
soft_penalty = (
    p_ang_momentum_z * w['ang_momentum_z'] + ... + step_penalty + no_step_penalty
) * penalty_scale
```

は、`training_progress`（jax配列であり`None`ではない）が常に`0.0`のため`progress_scale=0.0`となり、**上記の評価スクリプト全てで`soft_penalty`（エネルギー・滑らかさ・ドリフト・滑り・stance幅・歩行ペナルティ）が常時ゼロ**になります。`safety_penalty`（CBF・高さ/トルクバリア）は`step`ベースの`safety_scale`を使うため影響を受けませんが、**Gate 0の正式判定・Phase 0診断で報告される報酬値は、学習時とは異なる（ペナルティを欠いた）報酬関数で計算されている**ことになります。

さらに、正しく`TrainingProgressWrapper`を経由する学習中であっても、`training_progress`は学習全体（10M〜25Mステップ）にわたって0→1へ**線形**にしか増加しないため、学習序盤の大部分で`penalty_scale`はほぼ0に近い値になります。`PENALTY_INTRA_EPISODE_WARMUP_STEPS=30`という短いエピソード内ウォームアップとは別に、学習run全体を覆う長大な「実質ペナルティなし期間」が意図されたものかは、コード上何の説明もありません。

### V. `mass_scale`/`fric_scale`/`com_offset` のドメインランダム化が物理モデルに一切反映されていない

`envs/mjx_env.py::reset()`：

```python
mass_scale = jax.random.uniform(key_mass, shape=(), minval=RobotConfig.RANDOM_MASS_SCALE[0], maxval=RobotConfig.RANDOM_MASS_SCALE[1])
fric_scale = jax.random.uniform(key_fric, shape=(), minval=RobotConfig.RANDOM_FRICTION[0], maxval=RobotConfig.RANDOM_FRICTION[1])
com_offset = jax.random.uniform(key_com, shape=(3,), minval=RobotConfig.RANDOM_COM_OFFSET[0], maxval=RobotConfig.RANDOM_COM_OFFSET[1])

privileged_obs = jp.concatenate([jp.array([mass_scale, fric_scale]), com_offset, servo_temp, supply_volt])

mjx_data = mjx.make_data(self._mjx_model)   # ← self._mjx_model は __init__ で1度だけ構築、質量・摩擦・COMは不変のまま
```

`mass_scale`/`fric_scale`/`com_offset`は毎エピソードサンプルされ`privileged_obs`（既知の通り最終観測には含まれない死にベクトル）に詰め込まれるだけで、`self._mjx_model.body_mass`・`geom_friction`・各bodyの慣性オフセットを書き換えるコードはリポジトリ全体のどこにも存在しません。`__init__`で構築された`self._mjx_model`は全resetで使い回されるため、**質量・摩擦・重心オフセットのランダム化は完全に無効**です。

これが特に問題なのは、`robot/config.py`のコメントです：

```python
RANDOM_MASS_SCALE = [0.97, 1.03]  # Phase 1: DR範囲を縮小して基本直立に集中
RANDOM_FRICTION = [0.7, 1.1]      # Phase 1: 摩擦変動を控えめに
RANDOM_COM_OFFSET = [-0.02, 0.02]  # Phase 1: 重心偏差を最小化
```

これらの値は「基本直立の学習に集中させるために意図的に範囲を絞った」という説明が付いていますが、**この値は元から一切物理シミュレーションに効いていない**ため、範囲を絞ろうが広げようが学習結果に何の影響も与えません。ドキュメント上の「Phase 1向けにDR範囲を調整した」というナラティブ自体が、実装が伴わない空文になっています（同じ`reset()`内で個別関節に効く`dr_damping`/`dr_friction`(joint単位)は`step()`で実際に`qfrc_applied`へ加算され機能しているため、「一部のDRだけ死んでいる」という気づきにくい構造です）。

## 🔴🔴 重大（新規）

### T. ONNXデプロイ・可視化ツールがdeterministic方策ではなくstochastic方策を使っており、しかも固定PRNGKeyを使い回している

`deploy/export_onnx.py`：

```python
make_inference_fn = ppo_networks.make_inference_fn(ppo_network)
inf_fn = make_inference_fn(params)   # deterministic引数省略 → Brax既定値 False

def predict(obs):
    dummy_rng = jax.random.PRNGKey(0)   # 毎回同じキー
    action, _ = inf_fn(obs, dummy_rng)
    return action
```

Braxの`make_inference_fn`が返す`make_policy(params, deterministic=False)`は既定でstochastic方策です。ここでは`deterministic=True`が渡されていないため、**実機に載せるONNXモデルの元になる関数が確率的サンプリングのまま**になっています。しかも`dummy_rng = jax.random.PRNGKey(0)`が**観測ごとに再生成される固定キー**であるため、真にランダムな探索ノイズにはならず、平均行動から一定方向にオフセットした「決め打ちバイアス」が常に加わる格好になります（`jax.random.normal(key, shape)`は同じ`key`なら同じ値を返すため）。

同種の問題は`train/visualize_rl.py::build_inference_fn()`にもあります：

```python
inference_fn = ppo_networks.make_inference_fn(ppo_network)
...
return jax.jit(inference_fn(params_stripped))   # ここも deterministic 未指定
```

一方`train/export_trajectory.py`は正しく`deterministic=True`を指定しており、**同じプロジェクト内で「決定論的評価をすべき」という意識とそうでない箇所が混在**しています。

### U. Gate 0正式評価スクリプトが`deterministic=False`をハードコードしており、プロジェクト自身の評価方針と矛盾する

`scratch/gate0_formal_eval.py`：

```python
policy_fn = jax.jit(make_policy(params_stripped, deterministic=False))
```

ところが`docs/master_plan.md`「Gate A以降の統計設計」には明記されています：

> 主判定はdeterministic評価、stochasticは参考指標

「Gate 0の正式評価（formal eval）」という名前を冠したスクリプト自身が、プロジェクトが定めた評価プロトコル（主判定=deterministic）に反してstochastic評価を主判定として使っています。これはS（training_progressの問題）とも重なり、Gate 0/Phase 0の合否判定そのものの信頼性に関わる問題です。

### W. FSRの左右割り当てが、XML/報酬計算側と config.py/実機側で逆になっている

`humanoid.xml`のセンサー宣言順は左→右です：

```xml
<touch name="sensor_l_foot_fl" .../>
<touch name="sensor_l_foot_fr" .../>
<touch name="sensor_l_foot_bl" .../>
<touch name="sensor_l_foot_br" .../>
<touch name="sensor_r_foot_fl" .../>
...
```

`envs/mjx_rewards.py::compute()`はこの順序を前提に命名しています：

```python
left_foot_force  = ... data.sensordata[0:4] ...
right_foot_force = ... data.sensordata[4:8] ...
```

（既知の bugA により実際のインデックスは別物を拾っていますが、Aを修正した場合にこの命名は正しくXML順と一致します。）

ところが`robot/config.py`の`FSR_POSITIONS`は逆の順序でコメントされています：

```python
FSR_POSITIONS = np.array([
    [-0.08, -0.04], [0.08, -0.04], [-0.08, 0.04], [0.08, 0.04],  # Right foot
    [-0.08,  0.04], [0.08,  0.04], [-0.08, -0.04], [0.08, -0.04],  # Left foot
])
```

そして`real/real_env.py`もこの「先頭4=右」という規約に合わせています：

```python
right_contact = np.any(fsr_raw[:4] > 0.5)
left_contact = np.any(fsr_raw[4:] > 0.5)
```

つまり**「先頭4要素が右足か左足か」という規約が、シミュレーション側（XML＋mjx_rewards.py）と設定/実機側（config.py＋real_env.py）で正反対**になっています。bugAとは独立に存在する問題で、Aだけを修正すると今度はZMP計算（`mjx_env.py`の`foot_positions = jp.array(RobotConfig.FSR_POSITIONS)`と`fsr_data`の掛け合わせ）で左右の位置と力が入れ替わったまま計算されることになります。

### Y. GPU台数に応じたバッチ自動調整の整数演算が、自ら課した割り切れ条件を満たせないことがある

`train/train_mjx.py`：

```python
if (batch_size * num_minibatches) % num_envs != 0:
    import math
    target_total_batch = math.ceil((batch_size * num_minibatches) / num_envs) * num_envs
    num_minibatches = target_total_batch // batch_size
    print(f"...num_minibatches を {num_minibatches} に自動調整しました...")
```

`target_total_batch`は`num_envs`の倍数になるよう計算されますが、**`batch_size`の倍数になる保証がありません**。例えば`batch_size=100, num_minibatches=4, num_envs=150`の場合：

```
target_total_batch = ceil(400/150)*150 = 450
num_minibatches    = 450 // 100 = 4   （整数除算で切り捨て、変化なし）
最終的な batch_size*num_minibatches = 400
400 % 150 = 100 ≠ 0   ← 条件は満たされないまま
```

コード自身のコメントが明言する「batch_size * num_minibatches は num_envs で割り切れる必要がある」という不変条件を、この自動調整ロジックは一般には満たせません。デフォルト値（256/256/16、GPU1枚）では偶然割り切れるため潜伏していますが、マルチGPUや`--num_envs`/`--batch_size`/`--num_minibatches`のカスタム指定次第で顕在化しうる実装バグです。

## 🟡 中程度（新規）

### Z. `no_step_penalty` が到達不能な条件で書かれており、常にゼロにしかならない

`envs/mjx_rewards.py`：

```python
no_step_penalty = jp.where(
    getattr(RobotConfig, 'ALLOW_WALKING', False) or getattr(RobotConfig, 'ALLOW_STEPPING', False),
    100.0,
    0.0,
)
```

これは「ALLOW_WALKING/ALLOW_STEPPINGが**True**のとき100.0のペナルティを課す」というロジックです。しかし`envs/mjx_env.py::step()`側には：

```python
if getattr(RobotConfig, 'ALLOW_WALKING', False) or getattr(RobotConfig, 'ALLOW_STEPPING', False):
    raise ValueError("Walking/stepping is forbidden for this task.")
```

というガードが**先に**あり、これらのフラグがTrueなら報酬計算まで到達する前に例外で落ちます。つまり`no_step_penalty`が非ゼロになりうる分岐（True側）は絶対に実行に到達せず、現在の設定（両方False）では常に0.0にしかなりません。「実際に歩行・踏み出しをした場合にペナルティを課す」という意図だったなら、`body_vel_xy`や`foot_translation`のような実測値に基づく判定にすべきところ、設定フラグの真偽をチェックするコードになっており、機能していません（実質的な歩行抑制は既存の`step_penalty`だけが担っています）。

### X. 報酬ハッキング監査の「Exploitation」しきい値が、実際に取りうる値の範囲より一桁以上大きく、事実上発火しない

`train/train_mjx.py`：

```python
REWARD_AUDIT_THRESHOLDS = {
    'exploitation_abs_max': 50.0,
    ...
}
component_suffixes = [
    'r_cp', 'r_recovery', 'r_upright', 'r_com_stab',
    'disturbance_recovery_bonus', 'pbrs_reward', 'alive',
]
for suffix in component_suffixes:
    ...
    if abs(val) > th['exploitation_abs_max']:
        alerts.append(f"[Exploitation] 報酬成分 '{key}' が異常に大きい...")
```

しかし`metrics`辞書に格納されているのはいずれも**重み付け前の生値**です：`alive`は常に`1.0`固定、`r_upright`/`r_com_stab`/`r_cp`は`exp(...)`で`[0,1]`、`r_recovery`は`[0,5.0]`にクリップ、`disturbance_recovery_bonus`も`[0,10.0]`にクリップ。`pbrs_reward`（PBRS由来）ですら理論上限は`compute_potential()`のΦの上限（`w['upright']+w['target_pose']`≒16）程度です。REWARD_WEIGHTSが今の値のままである限り、これらの値が50.0を超えることは数学的にあり得ず、**「Reward Exploitation」検出は常に発火しない、実質ノーオペレーションのチェック**になっています。

### AA. `resolve_curriculum_schedule()` がどこからも呼ばれていない、3つ目の重複カリキュラム実装

`robot/config.py`にはカリキュラムスケール計算が3系統存在します：`mjx_env.py::_get_curriculum_scale()`、`mjx_rewards.py::_get_curriculum_disturbance_scale()`（既知の重複、両者は実装が同一）、そして`RobotConfig.resolve_curriculum_schedule()`：

```python
@classmethod
def resolve_curriculum_schedule(cls, total_steps: int = None) -> dict:
    """CURRICULUM_SCHEDULE_FRACTIONS を絶対ステップ数の辞書へ変換する。
    train_mjx.py 側で...呼び出し、envs/mjx_env.py へ供給することを推奨する。"""
    total = total_steps if total_steps is not None else cls.TOTAL_TRAINING_STEPS_ESTIMATE
    return {int(frac * total): scale for frac, scale in cls.CURRICULUM_SCHEDULE_FRACTIONS.items()}
```

docstringは「train_mjx.py側で呼び出すことを推奨する」と書いていますが、`train/train_mjx.py`のどこからもこのメソッドは呼ばれていません。3つ目の未使用カリキュラム実装が、使われないまま残っています。

### AB. `stability_metrics.py`の「フェーズ情報なし」フォールバックが構造的に到達不能

```python
is_single_support = jp.logical_or(
    jp.logical_and(gait_phase > 0.2, gait_phase < 0.8),
    gait_phase < 0.0  # フェーズ情報がない場合はデフォルト
)
```

コメントは「フェーズ情報がない場合」に負の番兵値が渡される想定ですが、`gait_phase`の呼び出し元は`envs/mjx_rewards.py`の`gait_phase=jp.asarray(info.get('phase', 0.0), dtype=jp.float32)`のみで、`info['phase']`は`USE_REFERENCE_GAIT=False`なら常に`0.0`（`mjx_env.py::step()`の`info['phase'] = 0.0`）、Trueでも`[0,1)`の範囲しか取りません。**負の値がどこからも供給されないため、この「フォールバック」分岐は最初から到達不能**です。現状（gait_phase=0.0固定）では常に「両足支持」重み付け（`w_cp=0.25, w_zmp=0.35, w_balance=0.25, w_orient=0.15`）に落ち着くため実害は目立ちませんが、ドキュメントされた挙動と実装が乖離しています。

### AD. CPUテスト時の`episode_length`（50）と`RobotConfig.MAX_EPISODE_STEPS`（500）の不一致で、内部のtime_out/truncatedが検証できない

`train/train_mjx.py`：

```python
episode_length = args.episode_length or 50        # CPU
episode_length = args.episode_length or RobotConfig.MAX_EPISODE_STEPS  # GPU (=500)
```

一方`envs/mjx_env.py::step()`の打ち切り判定は常に定数を直接参照します：

```python
truncated = info['step'] >= RobotConfig.MAX_EPISODE_STEPS   # 常に500
info['time_out'] = truncated.astype(jp.float32)
```

CPUモードでは`ppo.train(episode_length=50, ...)`によりBraxの`EpisodeWrapper`がstep=50で強制リセットしますが、環境自身の`info['truncated']`/`info['time_out']`は500に達するまで一度もTrueになりません。CPU上での小規模検証（`docs/master_plan.md`のTask2が要求する「done/truncated/time_out」の単体テストなど）を、実際にはこのパスで`time_out`が発火しない状態のまま実行してしまう可能性があります。

## 🟢 軽微（未解消の既知課題の再確認）

### AC. `tests/test_gui.py` が今も存在しない`envs/base_env.py`を参照している

```python
from envs.base_env import MuJoCoSim
```

現在の`envs/`ディレクトリ構成には`__init__.py`, `actuator_model.py`, `mjx_env.py`, `mjx_rewards.py`, `stability_metrics.py`, `training_wrapper.py`しかなく、`base_env.py`は存在しません。これは`docs/old/CODE_IMPROVEMENT_REPORT_v2.md`で「stale test」として自己申告済み（削除するか判断待ちと記載）ですが、今回渡されたファイル一覧を見る限り**未だに解消されていません**。pytestで`tests/`ディレクトリ全体を回すと収集時に`ModuleNotFoundError`で落ちます。

---

## まとめ

優先度で並べると、直すべき順は概ね以下になります。

1. **V**（質量/摩擦/COMのDRが完全に無効）— Phase 1のドキュメント上の意図と実装が根本から乖離
2. **S**（`training_progress`が評価スクリプトで凍結）— Gate 0/Phase 0の合否判定そのものの信頼性に関わる
3. **U・T**（評価・デプロイでdeterministic方策が使われていない）— 「学習は正しくても評価・実機投入の入口で別物になる」系統の問題
4. **W**（FSR左右規約の逆転）— bugAと合わさるとsim側の左右足力・位置対応が二重に壊れる
5. Y・Z・X・AA・AB・AD・AC は個別の実害は限定的ですが、いずれも「意図した通りに動いていない／到達しないコード」であり、今後別の変更を加えた際に静かに悪さをする類の負債です。
これまで(A〜AD)は環境実装・XML・実機通信・依存関係が中心でした。今回は**PPO学習ループ・報酬設計・監視ロジックそのもの**を掘り下げます。新規に見つかったものだけを報告します。

## 🔴🔴🔴 最重大（新規・実行すれば即座に確認できる）

### AE. `scratch/verify_changes.py` の報酬重み検証が、現在の`config.py`と完全に食い違っている

```python
# scratch/verify_changes.py
w = RobotConfig.REWARD_WEIGHTS
assert w['alive'] == 2.0, f"alive should be 2.0, got {w['alive']}"
assert w['fall_penalty'] == -20.0, f"fall_penalty should be -20.0, got {w['fall_penalty']}"
```

対して`robot/config.py`の実際の値：

```python
REWARD_WEIGHTS = {
    "alive": 25.0,
    "fall_penalty": -30.0,
    ...
}
```

`alive`は`2.0`ではなく`25.0`、`fall_penalty`は`-20.0`ではなく`-30.0`です。**このスクリプトは実行した瞬間、最初の`assert`で`AssertionError`を吐いて停止します**。ファイル冒頭のdocstring「Verify the reward weight changes and import consistency」が意図する「報酬重み変更の整合性確認」という役割を、このスクリプト自身がもう果たせていません。おそらく`alive=2.0/fall_penalty=-20.0`だった旧世代の`REWARD_WEIGHTS`を検証するために書かれ、その後`config.py`側だけがPhase 1向けに`25.0/-30.0`へ更新され、検証スクリプトの更新が漏れた状態です。CIやpre-commitでこのスクリプトを回している場合、常に赤信号のまま放置されていることになります。

## 🔴🔴 重大（新規）

### AF. 物理発散(physics_diverged)による強制終了は、転倒(fall_penalty)としてカウントされずに報酬だけ「無傷」で終わる

`envs/mjx_rewards.py::compute()`内で`done`と`total_reward`が確定する順序を追うと：

```python
done = jp.logical_or(jp.logical_or(is_fallen_roll, is_fallen_pitch), is_low)
...
done = jp.logical_or(done, reward_is_finite < 0.5)
total_reward = jp.where(reward_is_finite > 0.5, total_reward, w['fall_penalty'])
total_reward = jp.clip(total_reward, -300.0, 300.0)
total_reward = jp.where(done, w['fall_penalty'], total_reward)   # ← ここで fall_penalty が確定
return total_reward, done, metrics, current_potential
```

この時点の`done`は「roll/pitch/height超過」または「reward自体がNaN/Inf」しか知りません。ところが`envs/mjx_env.py::step()`側では、これより**後**で物理発散フラグを合成しています：

```python
reward, done, metrics, current_potential = self._reward_system.compute(...)   # reward は既に確定済み
info['last_potential'] = current_potential

done = jp.logical_or(done, physics_diverged)   # ← done だけが後から更新される
metrics['physics_diverged'] = physics_diverged.astype(jp.float32)
```

つまり `physics_step()` の数値発散ロールバック機構（NaN/Inf対策として実装済み）が作動して`physics_diverged=True`になった場合、**`terminated=True`（エピソードは強制終了する）にもかかわらず、そのステップの`reward`は`fall_penalty`に置き換わらず、ロールバック後の「見た目正常な状態」から計算された通常の（時には比較的高い）報酬のまま**エージェントに渡ります。

転倒(roll/pitch/height)による終了と、数値発散による終了は、`done`としては区別なく扱われるのに、**報酬としては全く違う扱い**を受けるという不整合です。PPOの学習信号としては「物理が壊れてエピソードが打ち切られても、別に罰は受けない」という誤った因果を学習しうる余地があります。

### AG. `action_history`のゼロ初期化が、全エピソードの開始直後に確率的な「目標角0ラジアン指令」を発生させる

`reset()`では`action_history`を全ゼロで初期化します：
```python
'action_history': jp.zeros((RobotConfig.HISTORY_LEN, self._mjx_model.nu)),
```
`HISTORY_LEN=5`。`step()`内の遅延サンプリング：
```python
ah = jp.roll(info['action_history'], shift=-1, axis=0)
ah = ah.at[-1].set(real_target_rad)
info['action_history'] = ah
...
delay_idx = jax.random.randint(rng_delay, shape=(), minval=0, maxval=3)  # 0,1,2 一様
applied_action = ah[RobotConfig.HISTORY_LEN - 1 - delay_idx]
```

エピソード**1step目**では`ah = [0,0,0,0, real_target_rad]`。`delay_idx=1`なら`ah[3]=0.0`、`delay_idx=2`なら`ah[2]=0.0`が選ばれます（確率2/3）。**2step目**でも`delay_idx=2`なら依然`ah[2]=0.0`が選ばれます（確率1/3）。3step目以降はようやく全インデックスが実際の指令値で埋まり、この問題は消えます。

このアクチュエータは`<position>`型（絶対角度指令、残差ではない）なので、`applied_action=0.0`が選ばれると、たとえば`right_knee`（`DEFAULT_JOINT_ANGLES`では`-0.58 rad`が標準姿勢）に対して**膝を伸ばしきる方向へ突然指令する**ことになります。これは方策の良し悪しとは無関係な、初期化バッファの構造に起因する物理的な不連続です。

`docs/status.md`が繰り返し記録している「学習後半で`episode_alive`が低下する／KLスパイクが残る」という未解決症状の直接の説明にはなりませんが、**全エピソードの最初の1〜2stepで方策と無関係な外乱的ショックが確率2/3〜1/3で毎回入る**という事実は、`episode_alive`の統計にノイズを乗せ、診断結果の解釈を歪める要因になり得ます。ここは注意喚起として記録する価値があります。

## 🟡 中程度（新規）

### AH. 報酬ハッキング監査の "Shaping Mismatch" が、むしろ正常な学習初期に頻発する（既知の"Exploitation"閾値問題の逆側）

```python
REWARD_AUDIT_THRESHOLDS = {
    'shaping_high_reward': 10.0,
    'shaping_low_positive': 1.0,
    ...
}
...
positive_sum = max(r_upright, 0.0) + max(r_com_stab, 0.0) + both_feet_contact
if total_reward > th['shaping_high_reward'] and positive_sum < th['shaping_low_positive']:
    alerts.append(f"[Shaping Mismatch] 高報酬...")
```

`r_alive * w['alive'] = 1.0 * 25.0 = 25.0`だけで既に閾値`10.0`を超えます。一方`positive_sum`（`r_upright + r_com_stab + both_feet_contact`、いずれも重み無しの生値で`[0,1]`程度）は、学習初期でまだバランス感覚を獲得していない方策では容易に`1.0`を下回ります。つまり**「まだ立ち方を学習していない、ごく普通の学習初期状態」で、このアラートは恒常的に発火します**。これは実際の異常（報酬設計ミス・reward hacking）を検出するための仕組みが、想定外に頻発する誤検知でログを埋め尽くし、本当に見るべき異常を覆い隠す方向に働きます。

以前確認済みの「Exploitation」閾値（`50.0`、実際の報酬成分は理論上限的に超えないため一度も発火しない）と合わせると、**この監査機構は"閾値が甘すぎて発火しない項目"と"閾値が厳しすぎて発火しすぎる項目"の両方を抱えており、現在のREWARD_WEIGHTSの実際のスケール感と閾値設計がそもそも噛み合っていない**ことが分かります。

### AI. NaN検知の`training/total_reward`フォールバックキーが、実在しない場合に学習を誤って停止させうる

```python
reward = metrics.get('eval/episode_reward', metrics.get('training/total_reward', float('nan')))
...
if not np.isfinite(reward):
    raise RuntimeError(f"NaN/Inf detected in reward at step {num_steps}. ...")
```

`progress_fn`が呼ばれた際に`eval/episode_reward`キーが存在しない場合、`training/total_reward`にフォールバックしますが、**このキー名がBraxの実際のmetrics辞書に存在する保証はコード上どこにもありません**。もし両方とも存在しないタイミングで`progress_callback`が呼ばれると、`metrics.get(...)`は`float('nan')`を返し、直後の`np.isfinite(reward)`チェックにより**「NaN/Inf検出、改良規約§18により学習を即時停止する」という重大なエラーが誤発火**します。これは「学習が本当にNaN/Infを起こした」ことと「metricsキー名の想定が外れただけ」という全く別の事象を区別できておらず、**安全装置（NaN検知）自体の信頼性が、metrics辞書のキー名という脆い前提に依存している**という構造的な弱点です。

### AJ. PBRS(Potential-Based Reward Shaping)のΦ項が、終端ステップでは丸ごと捨てられfall_penaltyに置換される

```python
r_pbrs = gamma * current_potential - last_potential
...
total_reward = (
    r_alive * w['alive'] + r_pbrs + r_upright * w['upright'] + ...
) - soft_penalty - safety_penalty
...
total_reward = jp.where(done, w['fall_penalty'], total_reward)  # r_pbrs を含む計算結果を丸ごと上書き
```

PBRSの理論的な「最適方策への不変性」は、シェーピング項`F(s,a,s')=γΦ(s')-Φ(s)`を**すべての遷移に一貫して加算し続けること**が前提です。終端遷移でだけ計算済みの`r_pbrs`（`current_potential`の情報を含む）を丸ごと定数`fall_penalty`に置き換えるのは、シェーピングの一貫性という観点では例外的な扱いであり、「転倒直前まで積み上げてきたポテンシャル差分による学習信号」が最後の1ステップだけ完全に消え、代わりに固定ペナルティに置き換わる、という設計になっています。実害の大きさは重みのバランス次第ですが、意図した設計かどうかコード上に説明がなく、確認すべき箇所です。

### AK.（要検証・確度中）`best_params.pkl`のスナップショットタイミングが、評価対象のパラメータと厳密に一致する保証が読み取れない

```python
def policy_params_callback(current_step, make_policy, params):
    nonlocal current_params
    current_params = params          # 呼ばれるたびに上書き
    with open(run_dir / "last_params.pkl", "wb") as f:
        pickle.dump(params, f)

def progress_callback(num_steps, metrics):
    reward = metrics.get('eval/episode_reward', ...)
    ...
    if current_params is not None:
        if reward > best_reward:
            best_reward = reward
            with open(run_dir / "best_params.pkl", "wb") as f:
                pickle.dump(current_params, f)   # ← この時点の current_params
```

`policy_params_fn`と`progress_fn`がBrax内部でどのタイミング・順序で呼ばれるかがこのファイルだけからは確定できません。もし`policy_params_fn`が`progress_fn`より高頻度（例えば`progress_fn`のeval実行前後で複数回）呼ばれる実装だった場合、`best_params.pkl`に保存される`current_params`は「その`reward`を実際に達成したパラメータ」ではなく、**それより後に更新された別のパラメータのスナップショット**になっている可能性があります。Brax側の呼び出し順序を実際に確認するまでは断定できませんが、「best_params.pklが本当にbestなrewardを出したcheckpointか」という、Gate評価の前提そのものに関わる点なので、検証しておく価値があります。

---

もっとも実害が大きいのは **AE**（検証スクリプトが今は完全に壊れている＝現在使えない）と **AF**（物理発散が転倒として罰せられない＝termination原因と報酬の因果が崩れている）です。**AG**は`docs/status.md`が長年追いかけている`episode_alive`低下の謎に対する、これまで検討されていなかった角度からの仮説候補として記録しておく価値があります。
これまで(A〜AH)は主に「値のズレ」「実装ミス」を中心に見てきましたが、今回は**定義されているのに一度も呼ばれない/参照されない関数・クラス・定数**に絞って全ファイルを再走査しました。新規に見つかったものだけを報告します。

## 🔴 最大の発見：`robot/kinematics.py` の `LegKinematics` は実質まるごと死んでいる

`robot/gait_generator.py::GaitGenerator.__init__`：
```python
def __init__(self):
    self.ik = LegKinematics()   # ← インスタンス化するだけ
    self.stand_height = RobotConfig.GAIT_STAND_HEIGHT
    ...
```
しかし `GaitGenerator.get_joint_angles()` の実際のIK計算はこちらです：
```python
hip_r, knee_r, ankle_r = _simple_ik_leg(x_r, z_r, self.thigh_len, self.knee_len)
```
`_simple_ik_leg` は同じファイル内の**別の独立した関数**で、`self.ik.solve_leg(...)` は一度も呼ばれていません。JAX側の `jax_get_reference_trajectory()` も同様に `_simple_ik_leg` を直接呼んでおり、`LegKinematics` を経由しません。

つまり：
- `self.ik = LegKinematics()` — 生成されるだけで一切使われない属性
- `LegKinematics.solve_leg()` — 本体のIKロジックだが、`kinematics.py` 自身の `__main__` テストブロック以外から**一度も呼ばれない**
- `LegKinematics.solve_leg_batch()` — どこからも呼ばれない
- `LegKinematics.forward_kinematics()` — 自己テスト以外で未使用
- `LegKinematics.get_ankle_pitch_for_horizontal_foot()`（staticmethod）— 未使用。同じ計算式(`-(hip_pitch+knee_angle)`)が`_simple_ik_leg`内に**別途インラインで重複実装**されている
- モジュール直下の `ik_reach_check()` / `ik_safety_clamp()` — 自己テスト以外どこからもimportされない

**`robot/kinematics.py` は事実上「本番経路からは呼ばれない、自己完結したテストコードのためだけのファイル」**であり、実際のIKは`gait_generator.py`内に重複実装された`_simple_ik_leg`が担っています。前回報告した「G: GAIT_THIGH_LEN/KNEE_LENが実寸と乖離」も、実際にはこの死んでいる`LegKinematics`クラスに対する指摘であり、本番で使われている`_simple_ik_leg`側でも同じ`RobotConfig.GAIT_THIGH_LEN/GAIT_KNEE_LEN`を参照するため実害は変わりませんが、「2つの並行するIK実装のうち片方が完全なデッドコード」という構造自体が新しい発見です。

## 🟡 `robot/math_utils.py`：クォータニオン変換系の半分が完全に未使用

`quat_to_euler`（およびそのnumpy/jax版）以外は、モジュール自身の`if __name__=="__main__"`テストブロック以外からは一度も呼ばれていません：

- `euler_to_quat` / `euler_to_quat_numpy` / `euler_to_quat_jax` — `mjx_env.py`・`real_env.py`はどちらも「クォータニオン→オイラー角」の方向しか使わず、逆変換はどこにも出てこない
- `normalize_quaternion` — `rotate_vector_by_quaternion`内でのみ使用
- `quaternion_inverse` — 同上
- `quaternion_multiply` — 同上
- `rotate_vector_by_quaternion` — この関数自体、`mjx_env.py`・`real_env.py`・`stability_metrics.py`のどこからも呼ばれていない

つまり `normalize_quaternion → quaternion_inverse → quaternion_multiply → rotate_vector_by_quaternion` という一塊の依存グラフが、外部から見て**孤立した誰にも呼ばれないサブシステム**になっています。

## 🟡 `safety/cbf.py`：ドキュメントで自己申告済みの死にコードが2つ

```python
def compute_cbf_penalty_legacy(self, nominal_action, limit_lower, limit_upper):
    """[DEPRECATED] 旧実装。後方互換性のために保持。
    【使用禁止】代わりに compute_cbf_penalty(...) を使用。"""
```
`mjx_env.py::step()`は`compute_cbf_penalty`（新版）のみを呼び出しており、`compute_cbf_penalty_legacy`は本当に一度も呼ばれていません。同様に、モジュール直下の`analyze_cbf_violations()`（CBF介入頻度の統計分析関数）も、`train_mjx.py`・`mjx_env.py`・テストコードのどこからも呼ばれていない、宣言されただけのユーティリティです。

## 🟡 `robot/config.py`：新たに確認できた死んだ定数・メソッド

| 定義 | 状況 |
|---|---|
| `RobotConfig.print_config()`（classmethod） | 定義されているが、リポジトリ全体のどこからも呼ばれていない |
| `RobotConfig.INIT_JOINT_ANGLES = np.zeros(NUM_JOINTS)` | 初期姿勢には常に`DEFAULT_JOINT_ANGLES`が使われ、こちらは一度も参照されない |
| `RobotConfig.LATENCY_STEPS = 1` | `mjx_env.py`の`__init__(self, ..., latency_steps: int = 1, ...)`はこの定数を読まず、独自のデフォルト値`1`を直接ハードコードしている（既知の「`self.latency_steps`が`step()`内で未参照」問題とは別に、そもそも設定側の定数自体が誰にも読まれていない） |
| `RobotConfig.MOTOR_VOLTAGE = 11.1` | `envs/actuator_model.py::HX30HMModel`は同じ値`11.1`を`NOMINAL_VOLTAGE`として**独自に再定義**しており、config.py側のこの定数は誰も参照しない |
| `RobotConfig.ROBOT_NAME`, `RobotConfig.OUTPUT_DIR` | 使われているのは死んでいる`print_config()`内だけで、実質的に到達不能 |
| `RobotConfig.GAIT_SWAY_WIDTH = 0.03` | `gait_generator.py`のX-Z平面サイクロイド軌道はY方向（左右の重心シフト）を一切扱っておらず、この定数はどこからも参照されない |

## 🟢 追加で見つかった未使用import

既知リスト（`docs/old/ADDITIONAL_FINDINGS_CBF_BUG.md`に記載済みの`os`(config.py)/`Optional`(gait_generator.py)/`Union`(mjx_env.py)等）とは別に、そこには載っていない新規の未使用importを2件確認：

- `real/real_env.py`：`import os` — ファイル全体で`os.`は一度も呼ばれていない（既知の「`Dict`が未使用」とは別口の、同ファイル内2件目の死んだimport）
- `deploy/export_onnx.py`：`import jax.numpy as jp` — `predict()`や`load_brax_inference_fn()`は`jax.random.PRNGKey`など`jax.`直下しか使っておらず、`jp.`は一度も出てこない

---

まとめると、今回の探索で最も価値が高いのは**kinematics.pyのLegKinematicsクラスがまるごと死んでいる**という発見です。「GAIT_THIGH_LEN/KNEE_LENの実寸乖離」という以前の指摘の対象コードが、実は本番経路で一度も実行されていなかった——という点は、「どの発見がどのコードパスに実際に影響するか」を整理し直す上で重要な訂正になります。ｚ